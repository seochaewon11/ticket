# NOLI 퍼블리싱 → React(Vite/TS) 전환 작업계획

## 0. 목표 및 전제

- **프로젝트 경로**: `D:\채원\프로젝트001` (Vite 프로젝트를 이 경로에 직접 생성 — `org/`, `디자인시안/` 등 기존 폴더와 공존)
- **원본**: `org/` (바닐라 HTML/CSS/JS로 퍼블리싱 완료된 SPA, 화면 10개)
- **프레임워크**: React + Vite
- **언어**: TypeScript
- **라우팅**: `react-router-dom`, 베이스 경로 `/`
- **컴포넌트 위치**: `src/components`
- **페이지 위치**: `src/pages`
- **공통 레이아웃**: 헤더(app-header) / 하단 탭바(bottom-nav)를 공통 컴포넌트로 분리
- **스타일**: styled-components (CSS-in-JS)

기존 바닐라 JS 구조가 이미 "화면 = render() 함수 단위, 데이터 = id 기반 배열" 형태로 정리되어 있어 React 전환에 유리함 (`org/js/data.js` 상단 주석: "추후 React로 전환하기 쉽도록, 모든 리스트는 id를 가진 객체의 배열로 구성"). 이 구조를 최대한 그대로 승계한다.

---

## 1. 원본(org/) 구조 분석 요약

### 1-1. 파일 구성

```
org/
├── index.html          # SPA 진입점 (화면 10개 section 전부 포함)
├── {screen}.html       # 화면별 단독 미리보기 파일 (css/js는 아래 공용 폴더 참조)
├── css/
│   ├── common.css      # CSS 변수(디자인 토큰), 리셋, 공통 컴포넌트(버튼/칩/카드/뱃지 등)
│   └── {screen}.css    # 화면별 스타일
├── js/
│   ├── data.js          # 전역 데이터 + 세션/취향 상태
│   ├── router.js        # NOLI_ROUTER: .screen 요소 토글 방식 라우팅
│   ├── common.js         # NOLI_COMMON: 아이콘셋, 헤더/하단탭바 렌더, 클릭 위임
│   └── {screen}.js       # 화면별 render()
└── img/                 # ⚠ 사용처 없음 (css/js 어디서도 참조 안 됨, 레거시 파일 → 마이그레이션 불필요)
```

> ⚠️ `org/` 루트에 있는 `alarm.js`, `favorite.js`, `detail.js` 등 flat 파일들은 `org/css`, `org/js` 폴더가 생기기 전의 **중복 구버전**이다(index.html·`{screen}.html`은 전부 `css/…`, `js/…` 경로를 참조). 내용도 `org/js` 쪽이 최신이므로 React 전환 시 **`org/css`, `org/js`, `org/index.html`, `org/{screen}.html`만 원본으로 취급**하고 루트 flat 파일은 참고하지 않는다.
>
> ⚠️ 프로젝트 루트의 `assets/`, `js/`(즉 `D:\채원\프로젝트001\js`)도 `org/js`의 과거 스냅샷이라 최신 소스가 아니다. Vite 프로젝트 세팅 시 이름 충돌 여부만 확인하고 참조하지 않는다.

### 1-2. 화면(라우트) 목록 — `org/index.html`의 `<section id="screen-*">` 기준

| # | screen id | 화면명 | 원본 파일 | 비고 |
|---|---|---|---|---|
| 1 | splash | 스플래시 | splash.html/css/js | 로고 노출 후 1.8s 뒤 자동 전환 |
| 2 | login | 로그인 | login.html/css/js | 소셜(구글/카카오/네이버) + 이메일, 실제 OAuth 없음 |
| 3 | favorite | 취향설정 (3단계: 장르·분위기 → 아티스트 검색 → 완료) | favorite.html/css/js, favorite_artist.html, favorite_search.html, favorite_final.html(서브스텝 미리보기) | 한 화면 내부에서 step state로 3단계 전환 |
| 4 | main | 메인(홈) | main.html/css/js | 히어로, 추천 캐러셀, 아티스트 소식, 이달의 추천 |
| 5 | detail | 공연 상세 | detail.css/js, main_detail.html | 매칭카드/정보/시놉시스/캐스트 |
| 6 | calendar | 캘린더 | calendar.html/css/js | 캘린더 카드, 알람 리스트, 다가오는 공연 |
| 7 | alarm | 티켓오픈 알람설정 | alarm.html/css/js | 대상 카드 + 토글 리스트 |
| 8 | storage | 보관함 | storage.css/js, playlist.html(구 명칭) | 저장한 공연 / 최근 본 공연 그리드 |
| 9 | report | 리포트 | report.html/css/js | 트렌드 차트, 도넛 차트, 랭킹 리스트 |
| 10 | profile | 프로필 | profile.css/js, mypage.html(구 명칭) | 태그, 추억 그리드, 메뉴 |

### 1-3. 공통 로직 (`org/js/common.js`, `org/js/router.js`)

- `NOLI_COMMON.ICONS`: 아이콘 20여 개를 인라인 SVG 문자열로 보유 (heart, star, search, calendar, bookmark, chart, user, bell 등)
- `NOLI_COMMON.renderHeader({ back, title, backAction })`: 상단바 — 기본(NOLI 로고) / 뒤로가기+타이틀 두 가지 변형
- `NOLI_COMMON.renderBottomNav(activeKey)`: 하단 탭바 — 캘린더 · 보관함 · (중앙 플로팅) 홈 · 리포트 · 프로필, 5개 고정
- `NOLI_COMMON.bindCommonNav`: `data-action="go-screen"` / `data-action="nav-back"` 클릭 위임 → React에서는 각각 `useNavigate()` 호출로 대체, 위임 로직 자체는 불필요
- `NOLI_ROUTER.showScreen(name)`: `.screen` 클래스 토글 방식 라우팅 → React에서는 `react-router` 선언적 라우팅으로 완전히 대체
- `splash.js`의 `buildLogoSvg(uid)`가 `window.NOLI_ASSETS.buildLogoSvg`로 전역 노출되어 login 화면에서도 재사용됨 → React에서는 `<NoliLogo />` 공통 컴포넌트로 대체(그라디언트 id 충돌은 `useId()`로 해결)

### 1-4. 데이터 (`org/js/data.js`)

- `session` (로그인 여부), `userPreference` (닉네임/온보딩 진행도/선택 취향) — 화면 전환 흐름을 좌우하는 **핵심 상태** → 전역 상태로 승격 필요
- `categoryOptions`, `moodOptions`, `artistOptions` — 취향설정 정적 옵션 데이터
- `performances` — 공연 목록(히어로/추천/상세 정보 겸용, `matchReasonHtml`처럼 상세 전용 필드 포함)
- `savedShows`, `recentlyViewed`, `profileTags` 등 화면별 목데이터
- 공연 포스터 이미지는 실제 파일이 아니라 `img-ph` + `data-theme="wicked"` 같은 **CSS 그라디언트 placeholder**(`common.css` 참고)로 구현되어 있음 → 래스터 이미지 마이그레이션 불필요, `theme` 값을 prop으로 받는 `PosterPlaceholder` 컴포넌트로 대체
- 현재 `localStorage` 등 영속화 로직 없음(새로고침 시 초기값으로 리셋) — React 전환 시에도 동일하게 메모리 상태로 유지(영속화는 범위 밖, 필요 시 별도 논의)

---

## 2. React 프로젝트 구조 설계

```
D:\채원\프로젝트001\
├── org/                         # 원본 퍼블리싱 리소스 (유지, 참고용)
├── 디자인시안/                    # 디자인 시안 이미지 (유지, 참고용)
├── index.html                    # Vite 엔트리
├── vite.config.ts
├── tsconfig.json
├── package.json
└── src/
    ├── main.tsx
    ├── App.tsx                   # BrowserRouter + Routes 정의
    ├── router/
    │   └── routes.tsx            # 경로 상수 + 라우트 테이블
    ├── pages/                    # 라우트 단위 컨테이너만 위치 (레이아웃 조립 + 데이터 연결)
    │   ├── SplashPage/
    │   ├── LoginPage/
    │   ├── FavoritePage/         # 단일 페이지, 내부 step state로 Mood → Artist → Complete 전환
    │   ├── MainPage/
    │   ├── DetailPage/
    │   ├── CalendarPage/
    │   ├── AlarmPage/
    │   ├── StoragePage/
    │   ├── ReportPage/
    │   └── ProfilePage/
    ├── components/
    │   ├── common/                # Header, BottomNav, Icon, NoliLogo, PosterPlaceholder, Layout
    │   ├── favorite/               # ProgressBar, CategoryGrid, MoodList, ArtistResults, CompleteSummary
    │   ├── main/                   # Hero, RecommendCarousel, ArtistNews, MonthlyPicks
    │   ├── detail/                  # MatchCard, InfoSection, Synopsis, CastList
    │   ├── calendar/                # CalendarCard, AlarmList, UpcomingList
    │   ├── alarm/                   # TargetCard, ToggleList
    │   ├── storage/                 # StorageCard
    │   ├── report/                  # TrendChart, DonutChart, RankList
    │   └── profile/                 # TagList, MemoryGrid, MenuList
    ├── context/
    │   └── AppStateContext.tsx    # session, userPreference + 액션(login, completeFavorite, toggleLike…)
    ├── data/                       # org/js/data.js를 타입 있는 모듈로 분리
    │   ├── categoryOptions.ts
    │   ├── moodOptions.ts
    │   ├── artistOptions.ts
    │   ├── performances.ts
    │   ├── savedShows.ts
    │   ├── recentlyViewed.ts
    │   └── profileTags.ts
    ├── types/
    │   └── index.ts               # Performance, Artist, CategoryOption, MoodOption, UserPreference, Session 등
    └── styles/
        ├── theme.ts                # common.css :root 변수를 styled-components 테마 객체로
        └── GlobalStyle.ts          # createGlobalStyle (리셋 + 공통 클래스 대체)
```

**네이밍 규칙**: 컴포넌트/페이지 파일은 `PascalCase.tsx`, 스타일 파일은 `ComponentName.styled.ts`로 컴포넌트와 같은 폴더에 배치. `pages/*`는 라우트 진입점 역할만 하고, 실제 UI 조각은 `components/{domain}/`에 둔다(요구사항의 "컴포넌트 위치: src/components, 페이지 위치: src/pages" 분리 원칙 준수).

---

## 3. 라우트 설계

| 경로 | 페이지 컴포넌트 | 레이아웃 |
|---|---|---|
| `/` | SplashPage | 없음(단독 풀스크린) — 진입 시 세션/취향 완료 여부에 따라 `/login` \| `/favorite` \| `/main`으로 자동 리다이렉트 |
| `/login` | LoginPage | 없음 |
| `/favorite` | FavoritePage (단일 라우트, 내부 `step` state로 화면 전환) | 없음(온보딩 전용) |
| `/main` | MainPage | Header + BottomNav(active: home) |
| `/detail/:performanceId` | DetailPage | Header(back) |
| `/calendar` | CalendarPage | Header + BottomNav(active: calendar) |
| `/alarm/:performanceId?` | AlarmPage | Header(back) |
| `/storage` | StoragePage | Header + BottomNav(active: storage) |
| `/report` | ReportPage | Header + BottomNav(active: report) |
| `/profile` | ProfilePage | Header + BottomNav(active: profile) |

- 원본은 `NOLI_ROUTER.showScreen()`으로 화면을 토글했지만, React에서는 **선언적 라우팅**으로 전환한다. 단, 취향설정(favorite)은 로그인 직후 진입하는 **단일 라우트 `/favorite`** 하나만 두고, 내부 `step` state로 화면을 전환한다 (원본 흐름과 동일: `favorite` → `favorite_artist` → `favorite_final`).
  - `FavoritePage` 내부에서 `step` 값에 따라 `components/favorite/`의 `MoodStep` → `ArtistStep` → `CompleteStep` 서브 컴포넌트를 조건부 렌더링(각각 원본 `favorite.html`, `favorite_artist.html`, `favorite_final.html`에 대응).
  - 완료(`CompleteStep`) 단계에서 "다음" 액션 시 `userPreference.completed = true` 갱신 후 `navigate("/main")`.
- 공통 `Header`의 뒤로가기(`onBack`)는 화면마다 목적지가 다르므로(storage/alarm → main 등) 각 페이지에서 `navigate("/main")`처럼 명시적으로 지정한다(원본 `bindCommonNav`의 `opts.onBack`과 동일한 패턴).
- `main.js`의 카드 클릭 → 상세 이동은 `navigate(`/detail/${id}`)`로 대체.

---

## 4. 상태 관리 전환

- `session`, `userPreference` → `AppStateContext` (React Context + `useReducer` 또는 `useState`)로 승격. 제공 값:
  - `session: { isLoggedIn }`, `login()`
  - `userPreference: { userName, userGreetingName, step, totalSteps, selectedCategories, selectedMoods, selectedArtists, completed }`, `updatePreference(patch)`, `completeFavorite()`
  - 찜/좋아요 토글(`isLiked`), 알람 on/off 등 화면 로컬 상호작용은 해당 페이지의 로컬 state로 충분 (전역 승격 불필요)
- 정적 옵션/목데이터(`categoryOptions`, `performances` 등)는 상태가 아니라 **읽기 전용 상수 모듈**(`src/data/*.ts`)로 그대로 이식.
- `NOLI_ASSETS.buildLogoSvg` 같은 전역 window 공유 트릭은 제거하고 공통 컴포넌트(`<NoliLogo uid="splash" />`)로 대체.

---

## 5. 스타일 전환 전략 (styled-components)

1. `org/css/common.css`의 `:root` 변수(`--color-primary`, `--radius-*`, `--space-*`, `--shadow-*`, `--app-max-width`, `--tabbar-height` 등) → `src/styles/theme.ts`의 타입 있는 테마 객체 **+** `GlobalStyle`에서 동일한 CSS 커스텀 프로퍼티로도 노출(점진적 이식과 디버깅 편의를 위해 이중 정의).
2. `common.css`의 리셋/공통 클래스(버튼, 칩, 카드, 뱃지, `.img-ph[data-theme=...]` 등)는 `GlobalStyle` 또는 `components/common`의 공용 styled 컴포넌트(`Button`, `Chip`, `Card`, `Badge`, `PosterPlaceholder`)로 변환.
3. 화면별 `{screen}.css`는 해당 페이지/컴포넌트 폴더의 `*.styled.ts`로 1:1 변환(선택자를 styled 컴포넌트로 매핑, BEM성 클래스명은 컴포넌트명으로 정리).
4. 인라인 SVG 아이콘 세트(`NOLI_COMMON.ICONS`)는 `components/common/Icon.tsx` 하나로 통합하거나, 아이콘별 개별 컴포넌트로 분리(`icons/HeartIcon.tsx` 등) — 개수가 많으므로 `Icon({ name })` 통합 방식을 우선 검토.

---

## 6. 작업 순서 (Phase)

1. **Phase 0 — 프로젝트 셋업**: `npm create vite@latest . -- --template react-ts`로 프로젝트 루트에 스캐폴딩, `react-router-dom`/`styled-components`/`@types/styled-components` 설치, 기존 `org`, `디자인시안`, `작업계획_퍼블리싱.md`와 경로 충돌 없는지 확인.
2. **Phase 1 — 디자인 토큰 & 공통 리소스**: `theme.ts`, `GlobalStyle.ts`, `Icon`, `NoliLogo`, `PosterPlaceholder` 이식.
3. **Phase 2 — 데이터 레이어**: `types/index.ts` 정의 → `data/*.ts` 이식(원본 `data.js` 값 그대로 포팅).
4. **Phase 3 — 전역 상태 & 라우터 뼈대**: `AppStateContext`, `App.tsx`(`BrowserRouter`+`Routes`), 공통 `Layout`(Header+BottomNav+Outlet).
5. **Phase 4 — 공통 컴포넌트**: `Header`, `BottomNav` 구현 및 스토리 수준 검증(각 상태별 렌더 확인).
6. **Phase 5 — 화면별 변환** (아래 순서로 원본 종속성 낮은 화면부터 진행):
   1. Splash (자동 리다이렉트 로직)
   2. Login (소셜 버튼 UI + `login()` 액션)
   3. Favorite (단일 페이지, `step` state로 Mood → Artist → Complete 3개 서브 컴포넌트 전환 + 진행바)
   4. Main (Hero/캐러셀/아티스트소식/이달의추천 + BottomNav 연결)
   5. Detail (Main에서 넘어온 id로 데이터 조회, 매칭카드/정보/시놉시스/캐스트)
   6. Calendar
   7. Alarm
   8. Storage
   9. Report (차트 2종 + 랭킹)
   10. Profile
7. **Phase 6 — 통합 QA**: 전체 플로우 수동 점검
   - 스플래시 1.8s 후 세션 상태별 분기(로그인 전/취향설정 미완료/완료)
   - 취향설정 3단계 진행 + 완료 후 메인 이동
   - 메인 → 상세 → 뒤로가기, 하단탭바 5개(캘린더/보관함/홈/리포트/프로필) 전 화면에서 정상 이동
   - 알람/보관함/캘린더/리포트/프로필 뒤로가기 목적지 확인
8. **Phase 7 — 정리**: 프로젝트 루트의 레거시 `assets/`, `js/`(구버전 스냅샷) 및 `org/` 루트의 flat 중복 파일 처리 여부를 사용자와 확인 후 정리(삭제는 별도 승인 필요).

---

## 7. 리스크 / 확인 필요 사항

- **`org/img/*.png`(25개)**: 현재 어떤 CSS/JS에서도 참조되지 않는 미사용 레거시 파일로 확인됨. React 전환 대상에서 제외.
- **소셜 로그인/알람/좋아요 토글 등은 실제 연동 없이 UI만 존재** — React 전환 후에도 동일하게 목업 동작 유지(실 API 연동은 범위 밖).
- **영속화(localStorage 등) 없음**: 새로고침 시 초기 데이터로 리셋되는 현재 동작을 그대로 유지할지, Context에 persist 미들웨어를 추가할지는 별도 결정 필요.

---

## 8. 완료 기준 (DoD)

- [ ] `npm run dev`로 10개 화면 모두 라우팅 정상 진입
- [ ] 원본 대비 시각적 diff 없음(디자인시안 13장 기준)
- [ ] 하단 탭바 5개 버튼이 모든 탭 진입 화면에서 활성 상태 표시 정상
- [ ] 취향설정 3단계 → 메인 진입 플로우 정상 동작
- [ ] `npm run build` 정상 완료, TypeScript 에러 0건
