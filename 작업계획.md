# NOLI 공연 큐레이션 앱 — React(Vite) + TypeScript 전환 작업계획

## 0. 요구사항 요약

| 항목 | 값 |
|---|---|
| 프로젝트 경로 | `D:\채원\프로젝트001\publising` (터미널 작업 디렉터리를 이 경로로 변경 후 Vite 프로젝트 생성) |
| 원본 위치 | `org/` (= `publising/org/`, 마이그레이션 완료 전까지 삭제하지 않고 참고용으로 보존) |
| 프레임워크 | React (Vite) |
| 언어 | TypeScript |
| 라우터 경로 | `/` (React Router `basename: "/"`) |
| 컴포넌트 위치 | `src/components` |
| 페이지 위치 | `src/pages` |
| 공통 컴포넌트 | 헤더, 푸터(하단 탭바) 분리 |
| CSS | 스타일 컴포넌트(styled-components) |
| 작업계획서 파일명 | `작업계획.md` |

> ⚠️ 요청 문구상 경로는 `D:\채원\project001`이었으나, 실제 디스크에는 동일한 폴더가 한글명 `D:\채원\프로젝트001`로 존재하며 영문 `project001` 폴더는 없습니다. 이 문서 전체에서는 실제 존재하는 `D:\채원\프로젝트001`을 프로젝트 경로로 사용합니다(오탈자/표기 차이로 판단, 다른 경로를 의도하셨다면 알려주세요).

> 원본은 `publising/org/` 아래 12개 화면(html) + 실제 사용 js 13개 + css 12개 + `img/`로 구성되어 있으며, Vite 프로젝트는 같은 `publising/` 루트에 `src/`, `public/`을 새로 생성해 진행합니다(`org/`와 신규 `src/`가 같은 루트에 공존).

---

## 1. 원본 소스 분석 결과

### 1-1. 대상 화면 12개

| 화면(html) | 제목 | 핵심 기능 |
|---|---|---|
| splash.html | 스플래시 | progress bar 애니메이션 후 자동 이동 |
| login.html | 로그인 | 소셜/이메일 로그인 버튼(실제 인증 없음, 클릭 시 이동만) |
| index.html | 오늘의 추천(홈) | 추천 공연 카드, 좋아요 토글, 보관함 담기, 플레이리스트 카운트 |
| detail.html | 공연 상세 | 포스터 히어로, 추천 이유, 상세정보, 시놉시스, 출연진 |
| add.html | 취향설정(1/3) | 장르/분위기 다중 선택 → 저장 |
| artist.html | 관심 아티스트(2/3) | 아티스트 검색/다중 선택 |
| complete.html | 설정 완료(3/3) | 취향 태그 요약 표시 |
| calendar.html | 공연 일정 | 월별 캘린더, 티켓 오픈 알람, 예정 공연 |
| alarm.html | 알람 설정 | 알람 대상 카드, 시간대별 토글 |
| play.html | 나의 공연 보관함 | 플레이리스트 카드, 최근 본 공연 |
| analysis.html | 상세 리포트 | 관람 트렌드 바 차트, 장르 도넛 차트, 아티스트 랭킹 |
| mypage.html | 마이페이지 | 프로필 카드, 취향 대시보드, 공연 기억, 메뉴 리스트 |

### 1-2. 실제 사용되는 스크립트 vs 미사용 파일 (⚠️ 중요, 실제 파일 grep으로 검증 완료)

각 `.html`의 `<script src>` 태그를 전수 조사한 결과, 실제로 로드되는 파일은 **`data.js` + 페이지별 js 12개(총 13개)** 뿐입니다.

- **마이그레이션 대상(13개)**: `data.js`, `splash.js`, `login.js`, `index.js`, `detail.js`, `add.js`, `artist.js`, `complete.js`, `calendar.js`, `alarm.js`, `play.js`, `analysis.js`, `mypage.js`
- **마이그레이션 제외(미사용, 어떤 html에서도 `<script>`로 참조되지 않음)**: `alarmsub.js`, `analysissub.js`, `artistsub.js`, `calendarsub.js`, `completesub.js`, `detailsub.js`, `mypagesub.js`, `playsub.js`, `date.js`
  - 실제로 `detailsub.js`를 열어 확인한 결과 `STORAGE_KEY = 'noli_app_data'`(구버전 키)이고, 현재 사용 중인 `data.js`는 `STORAGE_KEY = 'noli_app_data_v2'`입니다. 즉 `*sub.js` 9개 파일은 `data.js`의 **구버전 중복 사본**이 맞습니다. React 전환 시 로직 소스는 반드시 `data.js`(v2, 최신) 기준으로만 이식합니다.

### 1-3. 화면 간 이동 관계 분석 (현재 상태 그대로)

```
splash.js  --(2.7s 후 자동)-->  index.html
login.js   --(클릭, 실제 인증 없음)-->  index.html
add.js     --(다음 단계로)-->  index.html   ← ⚠️ artist.html을 건너뛰고 바로 홈으로 이동(미완성 연결)
artist.js  --(설정 완료)-->  complete.html
           --(뒤로가기)-->  add.html
complete.js --(두 버튼 모두)-->  index.html
calendar.js --(알람 카드 클릭)-->  alarm.html?id={alertId}   (쿼리스트링 방식)
           --(뒤로가기)-->  index.html
alarm.js   --(설정 완료/뒤로가기)-->  calendar.html
detail.js  --(뒤로가기)-->  index.html   / URLSearchParams로 ?id= 값 읽어 공연 조회
analysis.js --(뒤로가기)-->  mypage.html
mypage.js  --(로그아웃)-->  login.html   / (fabTicket 클릭)--> play.html
play.js    --(페이지 이탈 로직 없음, 독립 화면)
```

- `index.html`에는 `#detailBtn`(공연 상세 정보 보기) 버튼이 마크업엔 있으나 **현재 클릭 리스너가 연결되어 있지 않습니다.** → React 전환 시 정상적으로 `/detail/:id`로 연결 필요.
- 하단 탭바(`bottom-nav`)의 "활성(active)" 탭은 각 화면에 하드코딩되어 있으며, `add.html`은 5개 탭 중 **"탐색"** 탭이 active로 표시됩니다. `play.html`, `detail.html`은 실제 내용과 무관하게 "홈"이 active로 표시되어 있어, 탭 구조와 실제 라우트 매핑이 정리되어 있지 않습니다.
- 위 미완성/불일치 연결부는 **2번 섹션 "확인 필요 사항"**에서 정리하고, React 전환 시 라우터 레벨에서 명확히 재설계합니다.

### 1-4. 공통 UI 패턴 (컴포넌트 분리 대상)

- **하단 탭바(`bottom-nav`)**: index, detail, add, calendar, play, analysis, mypage 총 7개 화면에서 반복 사용 → 완전한 공통 컴포넌트화 대상(= "Footer")
- **상단 헤더/탑바**: 화면마다 형태가 다르지만 아래 패턴으로 그룹화 가능
  - **로고형 헤더**: index(로고+설정), calendar(뒤로가기+로고+알림), play/mypage(햄버거+로고+알림)
  - **뒤로가기+타이틀형 탑바**: add/artist(뒤로가기+미니로고+스텝바), alarm(뒤로가기+제목), analysis(뒤로가기+제목+공유)
  - **히어로 오버레이형**: detail(포스터 위에 뒤로가기/공유/알림 아이콘 오버레이)
- **스텝 프로그레스 바(`step-progress`)**: add/artist/complete 3개 화면 공용
- **카드(`card`), 버튼(`btn`, `btn-primary`, `btn-outline`), 배지(`badge`)**: `common.css`에 정의된 전역 공통 스타일 → styled-components 공통 컴포넌트로 대체
- **`data.js` API(20여 개 함수, localStorage 연동)**: 모든 페이지가 공통으로 의존하는 데이터 레이어
- **`common.css` `:root` 디자인 토큰** (실제 파일 확인 완료): 색상 15종(primary 계열 3, accent 계열 2, pink 계열 2, text 계열 4, surface 계열 4), radius 4종(`sm/md/lg/pill`), shadow 3종(`card/float/nav`), 폰트 `42dot Sans`

---

## 2. 확인이 필요한 사항 (⚠️ 진행 전 확인 권장)

아래 항목은 원본에 명시적 규칙이 없거나 원본 자체가 미완성인 부분입니다. 별도 답변이 없으면 각 항목의 "(가정)"대로 진행합니다.

1. **프로젝트 경로 표기** — 요청하신 `D:\채원\project001`은 실제로 존재하지 않으며, 동일 위치로 보이는 `D:\채원\프로젝트001`이 존재합니다. (가정) `D:\채원\프로젝트001\publising`을 프로젝트 루트로 사용.
2. **라우트 경로 체계** — "라우터 경로: `/`" 요구사항을 `BrowserRouter`의 `basename="/"` 설정으로 해석. (가정) 각 화면은 아래 개별 path를 가짐: `/`(splash) · `/login` · `/home` · `/detail/:id` · `/add` · `/artist` · `/complete` · `/calendar` · `/alarm/:id` · `/play` · `/analysis` · `/mypage`
3. **온보딩 흐름(add→artist→complete) 재연결** — 원본은 `add.js`가 `artist.html`을 건너뛰고 `index.html`로 이동합니다. (가정) React 버전에서는 `add → artist → complete → home` 순서로 **정상 연결**합니다.
4. **`index.html`의 상세보기 버튼 미연결** — (가정) `/detail/:id`로 정상 연결하고, 추천 카드가 보여주는 공연의 `id`를 그대로 전달합니다.
5. **로그인 처리** — 현재 실제 인증 로직 없이 버튼 클릭 시 무조건 `/home` 이동. (가정) React 전환에서도 동일하게 "실제 인증 없는 목업 이동"만 유지하고, 이후 인증 연동은 범위 밖으로 둠.
6. **하단 탭바 5개 탭과 페이지 매핑** — 탭: 홈/탐색/캘린더/분석/프로필. (가정) 홈→`/home`, 탐색→`/add`(온보딩 재진입 겸 탐색 화면으로 재사용), 캘린더→`/calendar`, 분석→`/analysis`, 프로필→`/mypage`. `play.html`(보관함)과 `detail.html`은 탭바 없이 개별 진입 화면으로 처리(필요 시 탭 활성 로직에서 제외).
7. **패키지 매니저** — (가정) `npm` 사용 (yarn/pnpm 선호 시 안내 부탁드립니다).
8. **이미지 자산 위치** — (가정) `org/img/*` → `public/img/`로 그대로 이관하여 mock 데이터의 `posterUrl` 등 기존 경로 문자열을 최대한 그대로 재사용(마이그레이션 리스크 최소화).

---

## 3. 기술 스택 최종안

| 영역 | 선택 | 비고 |
|---|---|---|
| 빌드 도구 | Vite | `npm create vite@latest . -- --template react-ts` |
| 언어 | TypeScript | strict 모드 |
| 라우팅 | react-router-dom v6 | `createBrowserRouter`, `basename: "/"` |
| 스타일 | styled-components | `common.css`의 `:root` 변수 → `theme.ts`, `ThemeProvider` 적용 |
| 상태관리 | React Context + Custom Hook | 별도 상태관리 라이브러리(Redux 등) 도입 없이 `data.js` 로직을 그대로 이식(localStorage persist 포함) |
| 아이콘 | 공통 `Icon` 컴포넌트 | 기존 인라인 SVG 문자열을 컴포넌트화하여 재사용 |

---

## 4. 폴더 구조 설계

```
publising/                          ← 프로젝트 루트 (Vite root)
├── org/                            ← 기존 퍼블리싱 원본 (마이그레이션 참고용, 유지)
├── public/
│   └── img/                        ← org/img 전체 이관
├── src/
│   ├── main.tsx
│   ├── App.tsx                     ← ThemeProvider + RouterProvider
│   ├── router.tsx                  ← 라우트 정의 (basename "/")
│   ├── components/
│   │   ├── layout/
│   │   │   ├── AppShell.tsx        ← .app 컨테이너(max-width 480px) 공통 레이아웃
│   │   │   ├── Footer.tsx          ← 공통 하단 탭바, 현재 라우트 기반 active 자동 판별
│   │   │   ├── LogoHeader.tsx      ← 로고형 헤더(설정/알림 아이콘 옵션)
│   │   │   ├── BackTopbar.tsx      ← 뒤로가기+타이틀/미니로고형 탑바
│   │   │   └── StepProgress.tsx    ← 3단계 스텝바(add/artist/complete 공용)
│   │   ├── common/
│   │   │   ├── Button.tsx          ← btn / btn-primary / btn-outline / btn-disabled
│   │   │   ├── Badge.tsx           ← badge-primary / badge-frost
│   │   │   ├── Card.tsx
│   │   │   └── icon/
│   │   │       ├── Icon.tsx        ← 공통 SVG 아이콘 컴포넌트
│   │   │       └── iconPaths.ts    ← 기존 인라인 SVG path 데이터 이관
│   │   └── (페이지 전용 하위 컴포넌트는 각 pages/**/components 로 배치)
│   ├── pages/
│   │   ├── Splash/SplashPage.tsx
│   │   ├── Login/LoginPage.tsx
│   │   ├── Home/HomePage.tsx (+ components/RecommendCard.tsx, PlaylistSummary.tsx)
│   │   ├── Detail/DetailPage.tsx (+ components/ReasonCard, InfoCard, CastList)
│   │   ├── Add/AddPage.tsx (+ components/GenreGrid, MoodTags)
│   │   ├── Artist/ArtistPage.tsx (+ components/ArtistGrid, SearchInput)
│   │   ├── Complete/CompletePage.tsx
│   │   ├── Calendar/CalendarPage.tsx (+ components/CalendarGrid, AlertScroll, UpcomingList)
│   │   ├── Alarm/AlarmPage.tsx (+ components/ReminderList)
│   │   ├── Play/PlayPage.tsx (+ components/PlaylistCard, RecentScroll)
│   │   ├── Analysis/AnalysisPage.tsx (+ components/TrendChart, GenreDonut, ArtistRanking)
│   │   └── MyPage/MyPage.tsx (+ components/ProfileCard, DashboardCards, MemoryGrid)
│   ├── styles/
│   │   ├── theme.ts                ← common.css :root 변수 이식 (색상/라운드/그림자/폰트)
│   │   ├── GlobalStyle.ts          ← createGlobalStyle (reset, body, .app 등)
│   │   └── styled.d.ts             ← DefaultTheme 타입 확장
│   ├── data/
│   │   ├── constants.ts            ← GENRE_OPTIONS, MOOD_OPTIONS, MOOD_TAG_MAP, GENRE_TAG_MAP, ARTISTS
│   │   ├── mockData.ts             ← DEFAULT_DATA, PLAYLISTS, RECENTLY_VIEWED, CALENDAR_EVENT_DATES, TICKET_OPEN_ALERTS, UPCOMING_PERFORMANCES, VIEWING_TREND, GENRE_DISTRIBUTION, TOP_ARTISTS_RANKING, TASTE_DASHBOARD, MEMORY_PERFORMANCES
│   │   └── NoliDataContext.tsx     ← data.js의 localStorage 연동 로직을 Context+Provider로 이식
│   ├── hooks/
│   │   └── useNoliData.ts          ← Context 소비 훅
│   └── types/
│       └── index.ts                ← Performance, User, UserPreference, Playlist, TicketAlert 등 타입 정의
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

---

## 5. 데이터 레이어 이식 계획 (`data.js` → TypeScript)

### 5-1. 타입 정의 (`src/types/index.ts`)

`DEFAULT_DATA.performances[]` 필드(공연마다 상세 화면용 필드 유무가 다름: `isRunning`, `tagline`, `recommendReason`, `cast` 등)를 분석해 `Performance` 타입은 상세 정보 필드를 **옵셔널**로 설계합니다. 그 외 `User`, `UserPreference`, `Playlist`, `TicketOpenAlert`(+ `ReminderOption`), `UpcomingPerformance`, `ViewingTrend`, `GenreDistributionItem`, `ArtistRanking`, `TasteDashboard`, `MemoryPerformance`, `Artist` 타입을 각각 정의합니다.

### 5-2. 함수 이식 매핑표 (`NoliData.*` → Context/Hook API)

| 원본 함수 | 사용 페이지 | 신규 위치(useNoliData 반환값) |
|---|---|---|
| `getData` | 전 페이지 초기화 | Provider 마운트 시 자동 초기화 (명시 호출 불필요) |
| `getUser` | index, complete, mypage | `user` |
| `getPerformances` / `getPerformanceById` | detail | `performances`, `getPerformanceById(id)` |
| `getRecommendedPerformances` | index | `getRecommendedPerformances()` |
| `getWishCount` / `getWatchedCount` | index | `wishCount`, `watchedCount` |
| `toggleLike` | index | `toggleLike(id)` |
| `addToWishlist` | index, detail | `addToWishlist(id)` |
| `markAsWatched` | (원본 미사용, API만 존재) | 동일하게 이식만 하고 미사용 유지 |
| `getUserPreference` / `updatePreference` | add, artist | `userPreference`, `updatePreference(genres, moods)` |
| `updateFavoriteArtists` | artist | `updateFavoriteArtists(ids)` |
| `generateTasteTags` | complete | `generateTasteTags()` |
| `getPlaylists` / `getRecentlyViewed` / `isPlaylistSaved` / `togglePlaylistSaved` | play | 동일명 유지 |
| `getCalendarEventDates` / `getTicketOpenAlerts` / `getUpcomingPerformances` | calendar | 동일명 유지 |
| `getAlertById` / `getAlertWithReminderState` / `toggleAlertReminder` | alarm | 동일명 유지 |
| `getViewingTrend` / `getGenreDistribution` / `getTopArtistsRanking` | analysis | 동일명 유지 |
| `getTasteDashboard` / `getMemoryPerformances` | mypage | 동일명 유지 |
| `GENRE_OPTIONS` / `MOOD_OPTIONS` / `ARTISTS` | add, artist | `src/data/constants.ts`에서 직접 import (Context 불필요, 정적 데이터) |

- `localStorage` 키(`noli_app_data_v2`)와 방어적 병합(구버전 데이터에 신규 필드 보강) 로직은 그대로 유지합니다.
- `recalculateMatchRates`는 `updatePreference` 내부 private 로직으로 이식(외부 미노출 유지).

---

## 6. 스타일(styled-components) 전환 계획

- `common.css`의 `:root` 변수 전체(색상 15종, radius 4종, shadow 3종, `--app-max-width: 480px`) → `src/styles/theme.ts`의 `theme` 객체로 이식, `ThemeProvider`로 전역 주입
- reset, `body`, `.app`, `button/a` 기본 스타일 → `GlobalStyle.ts` (`createGlobalStyle`)
- 42dot Sans 폰트 `@import` → `GlobalStyle.ts` 또는 `index.html`의 `<link>`로 유지
- 공통 클래스 → 공통 styled 컴포넌트로 1:1 대응
  - `.btn`, `.btn-primary`, `.btn-outline`, `.btn-disabled` → `<Button variant="primary" | "outline" | "disabled">`
  - `.badge-primary`, `.badge-frost` → `<Badge variant="primary" | "frost">`
  - `.card` → `<Card>`
  - `.bottom-nav`, `.nav-item` → `Footer.tsx` 내부에 캡슐화
  - `.step-progress` → `StepProgress.tsx` (props: `currentStep`, `totalSteps`)
- 페이지별 css(`splash.css`, `login.css`, `index.css` 등 12개) → 각 페이지 폴더 내 `styles.ts`(styled-components)로 1:1 이관, 기존 클래스명은 styled 컴포넌트명으로 의미 있게 재명명(BEM식 클래스 문자열 유지할 필요 없음)
- 인라인 스타일로 박혀 있던 `complete.html`의 도트 파티클, 그라디언트 문자열 등은 styled-components의 `css` prop 또는 컴포넌트 props로 전달

---

## 7. 라우팅 설계

```
/            → SplashPage      (진입 시 자동 리다이렉트, 2.7s 후 /home 또는 /login)
/login       → LoginPage
/home        → HomePage        (기존 index.html)
/detail/:id  → DetailPage
/add         → AddPage         (온보딩 1/3, 하단 탭 "탐색"에서도 재진입)
/artist      → ArtistPage      (온보딩 2/3)
/complete    → CompletePage    (온보딩 3/3)
/calendar    → CalendarPage
/alarm/:id   → AlarmPage       (기존 쿼리스트링 ?id= → path param으로 전환)
/play        → PlayPage
/analysis    → AnalysisPage
/mypage      → MyPage
```

- 기존 `window.location.href` / `window.history.back()` 이동은 모두 `useNavigate()` / `navigate(-1)`로 대체
- `detail.js`, `alarm.js`의 `URLSearchParams` 기반 id 조회는 `useParams<{ id: string }>()`로 대체(경로 파라미터 방식으로 통일, 2번 섹션 항목 2와 일관)

---

## 8. 작업 순서 (단계별 태스크)

1. **환경 세팅**: `publising/` 루트에서 Vite React-TS 템플릿 생성, `react-router-dom`/`styled-components`/`@types/styled-components` 설치
2. **디자인 토큰**: `theme.ts`, `GlobalStyle.ts` 작성 (common.css 이식)
3. **타입 정의**: `src/types/index.ts` 작성
4. **데이터 레이어**: `constants.ts`, `mockData.ts`, `NoliDataContext.tsx`, `useNoliData.ts` 구현 (localStorage 연동 포함, 단위 테스트 겸 임시 콘솔 확인)
5. **공통 컴포넌트**: `Button`, `Badge`, `Card`, `Icon`, `AppShell`, `Footer`, `LogoHeader`, `BackTopbar`, `StepProgress` 구현
6. **라우터 골격**: `router.tsx`, `App.tsx` 구성 (빈 페이지로 우선 라우팅 동작 확인)
7. **페이지 구현** (온보딩 플로우 우선 순서 권장): Splash → Login → Home(Index) → Add → Artist → Complete → Detail → Calendar → Alarm → Play → Analysis → MyPage
8. **에셋 이관**: `org/img/*` → `public/img/`, mock 데이터의 이미지 경로 검증
9. **통합 검증**: 전체 라우트 이동 흐름 점검(2번 섹션 가정사항 반영), 반응형(max-width 480px), 접근성(aria-label 등) 유지 확인
10. **빌드 검증**: `npm run build` 통과 확인, 디자인시안 폴더(`디자인시안/`) 대비 화면별 육안 비교

---

## 9. 완료 기준 (Definition of Done)

- [ ] 12개 페이지 모두 `src/pages`에 구현되고 라우터로 정상 진입 가능
- [ ] 헤더(로고형/뒤로가기형)와 푸터(하단 탭바)가 `src/components`의 공통 컴포넌트로 완전히 분리되어 재사용됨
- [ ] `data.js`의 모든 조회/변경 함수가 TS 타입과 함께 Context/Hook으로 이식되고 localStorage 연동 동작이 원본과 동일하게 유지됨
- [ ] 모든 페이지 스타일이 styled-components로 작성되고 전역 테마(`theme.ts`)를 공유함
- [ ] `npm run build` 성공, TypeScript 컴파일 에러 없음
- [ ] 2번 섹션의 확인 필요 사항에 대한 최종 결정 사항이 반영됨
