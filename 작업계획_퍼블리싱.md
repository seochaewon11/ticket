# NOLI 공연 큐레이션 앱 퍼블리싱 작업계획 (전체 화면 갱신판)

## 0. 배경 및 변경 사항

기존에 스플래시 / 취향설정(장르·분위기) / 메인 3개 화면만 퍼블리싱되어 있었는데, 이번에 전체 플로우 디자인 시안 13장이 추가로 공유되어 작업 범위를 다시 잡습니다.

**이미 만들어진 것**
- `index.html` (SPA 진입점) + `splash.html` / `favorite.html` / `main.html` (개별 리뷰용)
- `css/common.css`, `css/splash.css`, `css/favorite.css`, `css/main.css`
- `js/data.js`, `js/router.js`, `js/splash.js`, `js/favorite.js`, `js/main.js`

**이번에 새로 추가되는 화면 (업로드 이미지 기준)**
| 순번 | 이미지 파일 | 화면명 | 상태 |
|---|---|---|---|
| 1 | 1_스플래시 | 스플래시 | ✅ 완료 |
| 2 | 2_로그인 | 로그인 | 🆕 신규 |
| 3 | 3_취향설정_분위기 | 취향설정 (장르·분위기, Step 2 of 3) | ✅ 완료 |
| 4 | 4_취향설정_아티스트검색 | 취향설정 (아티스트 검색, Step 3 of 3 - 검색 상태) | 🆕 신규 |
| 5 | 5_취향설정_아티스트검색완료및선택 | 취향설정 (아티스트 검색, Step 3 of 3 - 결과·선택 상태) | 🆕 신규 (4번과 같은 화면의 다른 state) |
| 6 | 6_취향설정완료 | 취향설정 완료(분석 결과) | 🆕 신규 |
| 7 | 7_메인 | 메인 | ✅ 완료 (하단 탭바 라벨 수정 필요, 아래 참고) |
| 8 | 8_메인_상단content_클릭_상세화면 | 공연 상세화면 | 🆕 신규 |
| 9 | 9_캘린더 | 캘린더(공연 일정) | 🆕 신규 |
| 10 | 10_티켓오픈알람설정 | 알람설정 | 🆕 신규 |
| 11 | 11_보관함 | 보관함 | 🆕 신규 |
| 12 | 12_리포트 | 리포트 | 🆕 신규 |
| 13 | 13_프로필 | 프로필 | 🆕 신규 |

**⚠️ 기존 메인 화면 수정 필요 사항**
상세/캘린더/보관함/리포트/프로필 화면들을 보면 하단 탭바가 공통으로 `캘린더 · 보관함 · (중앙 플로팅) · 리포트 · 프로필` 4+1 구성인데, 기존에 만든 메인 화면은 라벨을 정확히 알 수 없어 `홈 · 브로드 · 리포트 · 프로필`로 임의 추정해 두었습니다. 이번 작업 때 전체 화면 공통 컴포넌트로 통일해서 **캘린더 / 보관함 / 리포트 / 프로필**로 교체합니다.

**⚠️ Step 번호 관련 참고**
장르·분위기 화면이 "Step 2 of 3", 아티스트 검색 화면이 "Step 3 of 3"으로 표시되어 있어 Step 1이 별도로 존재할 것으로 보이지만 해당 디자인은 전달되지 않았습니다. 임의로 만들지 않고, **디자인에 표시된 스텝 번호를 그대로** 재현합니다. Step 1 화면이 추가로 공유되면 그때 반영하겠습니다.

**⚠️ 소셜 로그인 아이콘 관련**
로그인 화면의 Google / 카카오 / 네이버 아이콘은 저작권이 있는 브랜드 로고이므로, 실제 로고 이미지를 그대로 복제하지 않고 **각 서비스를 연상시키는 색상·형태(원형 배지 + 이니셜/심볼)로 단순화해 재현**합니다. 실제 소셜 로그인 연동(OAuth)은 없고 UI만 동작합니다(클릭 시 로그인 완료 처리 후 다음 화면 이동).


## 1. 파일 구조 (확장판)

```
noli/
├── index.html                 # SPA 진입점 (전체 화면 section 포함)
├── splash.html / favorite.html / main.html   # 기존 개별 리뷰용 (유지)
├── login.html                 # 🆕 개별 리뷰용
├── detail.html                # 🆕 개별 리뷰용
├── calendar.html              # 🆕 개별 리뷰용
├── alarm.html                 # 🆕 개별 리뷰용
├── storage.html               # 🆕 개별 리뷰용
├── report.html                # 🆕 개별 리뷰용
├── profile.html                # 🆕 개별 리뷰용
├── css/
│   ├── common.css             # 기존 공통 변수/리셋/컴포넌트 (배지·차트 등 공통 요소 추가 예정)
│   ├── splash.css / favorite.css / main.css   # 기존
│   ├── login.css              # 🆕
│   ├── detail.css             # 🆕
│   ├── calendar.css           # 🆕
│   ├── alarm.css              # 🆕
│   ├── storage.css            # 🆕
│   ├── report.css             # 🆕
│   └── profile.css            # 🆕
├── js/
│   ├── data.js                # 기존 + 신규 데이터 대거 추가 (아래 3장 참고)
│   ├── router.js              # 기존 (신규 화면 id 추가만, 로직 변경 없음)
│   ├── splash.js / favorite.js / main.js     # 기존 (favorite.js는 3-스텝 내부 상태로 확장)
│   ├── login.js               # 🆕
│   ├── detail.js              # 🆕
│   ├── calendar.js            # 🆕
│   ├── alarm.js                # 🆕
│   ├── storage.js             # 🆕
│   ├── report.js              # 🆕
│   └── profile.js             # 🆕
└── assets/
```

**취향설정(favorite) 화면 구조 변경**
지금까지는 "장르·분위기" 1개 화면만 있었는데, 실제로는 같은 취향설정 플로우 안에 3개의 하위 단계가 있는 구조입니다.

```
favorite (취향설정 플로우)
 ├─ sub-step 1: 장르·분위기 선택          (Step 2 of 3) — 기존 화면
 ├─ sub-step 2: 아티스트 검색·선택         (Step 3 of 3) — 신규
 └─ sub-step 3: 완료(분석 결과 요약)        (스텝 표시 없음) — 신규
```
`favorite.js` 내부에 `subStep` 상태값을 두고 세 개의 render 함수(`renderMoodStep`, `renderArtistStep`, `renderCompleteStep`)를 전환하는 방식으로 확장합니다. (파일을 3개로 쪼개기보다, 하나의 취향설정 플로우 안에서 자연스럽게 다음 단계로 넘어가는 원래 UX를 그대로 반영하기 위함입니다.) 다만 화면별로 순서대로 요청하실 수 있도록, 이 문서에서는 "취향설정(아티스트)", "취향설정(완료)"를 별도 요청 단위로 안내드립니다.


## 2. 디자인 시스템 추가 토큰

기존 `common.css`의 보라 톤 변수는 유지하고, 새 화면들에 필요한 토큰을 추가합니다.

| 용도 | 값(추정) | 사용처 |
|---|---|---|
| `--color-danger-bg` / `--color-danger-text` | `#FDE2E2` / `#E0355B` | 캘린더 긴급 알람(D-3) 카드 |
| `--color-social-google` | 흰 배경 + 테두리 | 로그인 |
| `--color-social-kakao` | `#FEE500` | 로그인 |
| `--color-social-naver` | `#03C75A` | 로그인 |
| `--color-chart-track` | `#EDE4FB` | 리포트 막대/도넛 차트 배경 |
| `--color-profile-bg` | 보라 그라데이션 (`--gradient-primary` 재사용) | 프로필 화면 전체 배경 |

새로 필요한 공통 컴포넌트 클래스: `.badge-dday`(D-day 뱃지), `.toggle-switch`(알람 on/off 스위치), `.avatar-circle`(아티스트/출연진 원형 이미지), `.menu-row`(설정 리스트 행), `.chart-bar` / `.chart-donut`(리포트용 간단 차트) 등을 `common.css`에 추가합니다.


## 3. 데이터 구조 확장 (data.js)

기존 `userPreference`, `categoryOptions`, `moodOptions`, `performances`, `artistNews`, `monthlyPicks`에 아래 항목을 추가합니다.

```js
// 취향설정 - 아티스트 검색용 목데이터
const artistOptions = [
  { id: "leejoonyoung", name: "이준영", theme: "artist-1" },
  { id: "johseungwoo",  name: "조승우", theme: "artist-2" },
  { id: "leehonggi",    name: "이홍기", theme: "artist-3" },
  { id: "kimjunsu",     name: "김준수", theme: "artist-4" },
  { id: "v",            name: "뷔",     theme: "artist-5" },
  { id: "parkhaeil",    name: "박해일", theme: "artist-6" },
  { id: "taeyeon",      name: "태연",   theme: "artist-7" },
];

// userPreference 확장
userPreference.selectedArtists = ["parkhaeil"]; // artistOptions.id 참조
userPreference.userGreetingName = "채원";        // 완료 화면 등에서 사용하는 표시용 이름

// 공연 상세화면용 확장 필드 (performances 배열의 각 항목에 추가)
// matchReasonHtml, genrePreferenceText, trendingText,
// period, runningTime, priceRange, synopsis, castList: [{name, role, theme}]

// 캘린더
const calendarMarkers = [ { date: "2026-08-05", dot: "purple" }, ... ];
const ticketAlarms = [ { id, dday, title, datetime, urgent } ];
const upcomingShows = [ ... ]; // monthlyPicks와 유사 구조 재사용

// 알람설정 화면 상태
const alarmOptions = [
  { id: "24h", label: "24시간 전", checked: true },
  { id: "10h", label: "10시간 전", checked: false },
  { id: "1h",  label: "1시간 전",  checked: false },
];

// 보관함
const savedShows = [ ... ];       // performances 유사 구조, isLiked: true
const recentlyViewed = [ ... ];

// 리포트
const viewingTrend = [ { month: "9월", count: 4 }, ..., { month: "2월", count: 12 } ];
const genreDistribution = [ { label: "뮤지컬", percent: 45, color: "..." }, ... ];
const topArtists = [ { rank: 1, name: "김준수", role: "뮤지컬 배우", count: 4, theme: "..." }, ... ];

// 프로필
const memoryList = [ { id, title, date, theme } ]; // "나의 공연 기억" 카드
const profileMenu = [
  { id: "edit-preference", label: "취향 프로필 수정" },
  { id: "settings", label: "설정" },
  { id: "support", label: "고객센터" },
  { id: "terms", label: "공지사항 및 약관" },
];
```

모든 배열은 기존과 동일하게 `id` 기반 객체 배열로 관리해 React 전환 시 그대로 `.map()` 매핑이 가능하도록 합니다.


## 4. 화면별 상세 계획

### 4-1. 로그인 (login) — 🆕
- 보라 그라데이션 배경, 중앙 NOLI 로고 + 태그라인 "일상에 문화를 더하다, NOLI"
- 소셜 로그인 원형 버튼 3개(Google/카카오/네이버, 단순화된 아이콘)
- "이메일로 로그인" 버튼(보라, 메일 아이콘)
- 하단 텍스트 링크 "NOLI 회원가입 · 비밀번호 찾기"
- 아무 로그인 버튼 클릭 시 취향설정(장르·분위기) 화면으로 이동 (실제 인증 없음)

### 4-2. 취향설정 - 아티스트 검색 (favorite sub-step 2) — 🆕
- 상단 진행바 100%, "Step 3 of 3"
- 제목 "좋아하는 아티스트가 있나요?" + 설명
- 검색 입력창 (실시간 필터링, `artistOptions`를 이름 부분일치로 검색)
- 검색어 없을 때: 추천 아티스트 6명 그리드(3x2)
- 검색어 있을 때: 일치하는 아티스트만 리스트/그리드로 표시 (일치 0명/1명/여러명 모두 대응)
- 아티스트 클릭 시 다중 선택 토글 (선택 시 보라 링 강조), `userPreference.selectedArtists` 갱신
- 하단 "완료" 버튼: 선택 아티스트 1명 이상일 때만 활성화 → 완료 화면으로 이동

### 4-3. 취향설정 - 완료 (favorite sub-step 3) — 🆕
- 스텝 표시 없음, "설정이 완료되었습니다!" + 안내문
- 흰색 카드: "ANALYSIS" 라벨 + "OOO님을 위한 맞춤 공연을 준비했어요" + 선택한 카테고리/분위기/아티스트를 태그 칩으로 요약 표시
- 안내 문구 + "NOLI 시작하기" 버튼 → 메인 화면으로 이동, `userPreference.completed = true`

### 4-4. 메인 (main) — ✅ 완료, 하단 탭바만 수정
- 탭바 라벨을 `캘린더 / 보관함 / (중앙) / 리포트 / 프로필`로 교체
- 히어로 배너(공연 카드) 클릭 시 상세화면으로 이동하는 인터랙션 추가

### 4-5. 공연 상세화면 (detail) — 🆕
- 상단 back 버튼 + 로고
- 히어로 이미지 + 제목 + "나만의 보관함에 담기" 버튼(찜 토글)
- 취향일치 분석 카드(흰 카드, 히어로에 겹쳐 배치): 취향일치 %, 추천 이유 문구(키워드 강조), "장르 선호도" / "인기 급상승" 정보 행 2개
- "공연 상세정보": 기간 / 러닝타임 / 티켓 가격 (아이콘 + 라벨 + 값)
- "시놉시스": 본문 + "더보기" 토글(클릭 시 전체 텍스트 펼침/접기)
- "출연진": 가로 리스트(원형 사진 + 이름 + 배역)
- 하단 탭바 공통 컴포넌트

### 4-6. 캘린더 (calendar) — 🆕
- "공연 일정" 타이틀 + 안내문
- 월간 캘린더 카드: 이전/다음 달 이동 화살표, 요일 헤더, 날짜 그리드, 특정 날짜 점 표시(dot marker), 오늘/선택일 강조(보라 원)
- "티켓 오픈 알람" 섹션: D-day 뱃지(긴급은 빨간톤, 나머지는 보라톤) + 제목 + 일시 + 알람 아이콘 카드 리스트
- "예정된 공연" 섹션: 썸네일 + 카테고리 태그 + 날짜 + 제목 + 장소 리스트 (메인의 monthlyPicks 카드와 유사한 컴포넌트 재사용)

### 4-7. 알람설정 (alarm) — 🆕
- back + "알람설정" 타이틀 헤더
- 대상 공연 카드(썸네일 + 태그 + 제목 + 일시)
- 안내문(키워드 강조)
- 알람 시점 토글 3개(24시간 전 / 10시간 전 / 1시간 전) — 스위치 on/off
- 하단 "설정 완료" 버튼

### 4-8. 보관함 (storage) — 🆕
- "나의 공연 보관함" 타이틀 + 안내문
- 저장한 공연 3x3 그리드(각 카드 우상단 보라 하트 아이콘)
- "최근 본 공연" 3x3 그리드(하트 아이콘 없음)

### 4-9. 리포트 (report) — 🆕
- "관람 트렌드" 카드: 최근 6개월 관람 횟수 막대그래프(이번 달 "NOW" 뱃지 강조), 총 관람 횟수 표시
- "장르별 상세 분포" 카드: 도넛 차트 + 범례(장르별 퍼센트)
- "최애 아티스트 랭킹" 카드: 순위 배지 + 아티스트 사진 + 이름/역할 + 관람 횟수 리스트
- 하단 "리포트 친구에게 공유하기" 버튼 (공유 기능은 UI만, 실제 공유 로직은 alert 등으로 대체)

### 4-10. 프로필 (profile) — 🆕
- 페이지 전체 보라 그라데이션 배경 (다른 화면과 다른 톤)
- 아바타 + "오늘도 반가워요! OOO님!" 인사 + 검색 아이콘
- 취향 태그 칩 리스트(카테고리+분위기+아티스트 요약, `userPreference` 기반 동적 생성)
- "나의 공연 기억" 2x2 그리드(관람 기록)
- 흰 카드 메뉴 리스트: 취향 프로필 수정 / 설정 / 고객센터 / 공지사항 및 약관 (각 행 클릭은 자리만 유지, 실제 이동 없음)
- "로그아웃" 버튼 (클릭 시 로그인 화면으로 이동)


## 5. 작업 순서 제안

화면별로 순서대로 요청하신다고 하셨으니, 아래 순서를 추천드립니다. (원하시는 순서로 요청하셔도 됩니다.)

1. **공통 컴포넌트/데이터 보강** — `common.css`에 신규 토큰·컴포넌트 추가, `data.js`에 신규 데이터 일괄 추가, 메인 화면 하단 탭바 라벨 수정
2. **로그인**
3. **취향설정 - 아티스트 검색**
4. **취향설정 - 완료**
5. **공연 상세화면**
6. **캘린더**
7. **알람설정**
8. **보관함**
9. **리포트**
10. **프로필**


## 6. 확인 요청 사항

- 로그인의 소셜 버튼은 실제 로고 대신 색상·이니셜로 단순화해서 재현합니다 (저작권 이슈 회피 목적). 문제 없으실까요?
- Step 1이 없는 상태로 "Step 2 of 3" 문구를 디자인 그대로 유지해도 괜찮을까요? (추후 Step 1 화면을 주시면 반영하겠습니다)
- 캘린더/알람/차트 등은 실제 서버 연동 없이 목데이터로만 동작합니다 (버튼 클릭 시 화면 전환·상태 토글까지만 구현, 실제 알림 발송·서버 저장 없음). 이 정도 범위로 진행하면 될까요?
- 메인 화면 하단 탭바 라벨을 "캘린더/보관함/리포트/프로필"로 변경하는 것, 확인해주시면 다음 화면 작업 시작 시 같이 반영하겠습니다.

계획 확인해주시면 말씀하신 순서(또는 위 제안 순서)대로 화면별 코드를 작성하겠습니다.


---

## 7. 추가 요청 반영 — 아티스트검색 / 아티스트선택 / 완료 화면 (신규 첨부 3장 기준)

이번에 다시 첨부해주신 3장(`4_취향설정_아티스트검색`, `5_취향설정_아티스트검색완료및선택`, `6_취향설정완료`)은 바로 위 **4-2, 4-3번 항목과 동일한 화면**입니다. 다만 함께 주신 텍스트 요구사항 중 상당 부분이 **실제 첨부 이미지와 내용이 달라서**, 코드 작성 전에 먼저 확인을 요청드립니다. (요청하신 대로 이번 턴에는 코드를 작성하지 않고 계획만 정리했습니다.)

### 7-1. 이미지 기준 재확인 (이미 구현되어 검증까지 마친 상태)

세 화면은 이미 `favorite.js` 안에 서브스텝으로 구현해뒀고, 실제 브라우저에서 스크린샷을 찍어 이미지와 하나씩 대조 검증까지 마쳤습니다.

| 화면 | 구현 위치 | 상태 |
|---|---|---|
| 아티스트 검색 (검색 상태, 결과 1명) | `favorite.js` → `subStep === "artist"` (검색어 있음, 결과 1개일 때 중앙 정렬 카드) | ✅ 구현·검증 완료 |
| 아티스트 검색 (결과·선택 상태, 그리드) | `favorite.js` → `subStep === "artist"` (검색어 없거나 결과 여러 개일 때 3열 그리드) | ✅ 구현·검증 완료 |
| 완료(분석 결과) | `favorite.js` → `subStep === "complete"` | ✅ 구현·검증 완료 |

### 7-2. 요청 텍스트와 실제 이미지가 다른 부분 (확인 필요)

| 항목 | 요청 텍스트 내용 | 실제 첨부 이미지 내용 | 비고 |
|---|---|---|---|
| 파일 구성 | `favorite_search.html`, `favorite_arist.html`, `complete.html` 3개 파일로 분리 | 기존엔 `favorite.html` 하나에서 서브스텝(mood→artist→complete)으로 전환하는 구조로 이미 구현함 | 3개 파일로 다시 쪼갤지, 기존 구조를 유지할지 확인 필요 |
| `favorite_search.html` 내용 | "시놉시스 및 출연진 가로 스크롤", "'이전' / '공연 보관함에 담기' 버튼" | 아티스트 검색 화면(4번 이미지)에는 시놉시스·출연진이 없음. 이 내용은 오히려 **공연 상세화면(8번 이미지, `detail.js`)**의 구성 요소와 일치함 | 아티스트 검색 화면과 상세화면 설명이 서로 바뀐 것으로 보임 |
| 하단 탭바 라벨 | "홈, 탐색, 캘린더, 분석, 프로필" | 7~13번 이미지에서 공통으로 확인되는 라벨은 "캘린더, 보관함, (중앙 버튼), 리포트, 프로필" (이미 공통 컴포넌트로 구현함) | 어느 쪽 라벨이 맞는지 확인 필요 |
| 아티스트 선택 예시 | "선택된 아티스트(예: 홍광호)" | 5번 이미지에는 조승우/이홍기/김준수/뷔/박해일/태연 6명이 있고 '홍광호'는 없음 | |
| 진행바 단계 | "Step 2 of 3" | 4·5번 이미지에는 "Step 3 of 3"으로 표시되어 있음 | |
| 완료 화면 태그 | 해시태그 스타일 `#감성적인 #뮤지컬덕후 #라이브공연 #취향저격` | 6번 이미지에는 `#` 없는 pill 형태로 `콘서트·뮤지컬·다정한·아름다운·차가운 도시·박해일·이준영` 표시 | |
| 완료 화면 문구 | "이제 NOLI가 소연님의 취향을 찾았어요." | 6번 이미지에는 "NOLI가 **채원**님 취향을 찾았어요." (분석 카드 안에서는 "**소연**님을 위한…") | |
| 완료 화면 배경 | "파스텔톤 도트/파티클 효과" | 6번 이미지는 흰 배경, 별도 효과 없음 | |
| 작업순서 2번 | "공연상세화면(detail.html) 우선 작업" | 이번에 첨부하신 3장(검색/선택/완료)과는 무관한 다른 화면(8번 이미지) | |
| 데이터 관리 문구 | "습관 목록은 배열 형태로 관리" | NOLI는 습관(habit) 앱이 아니라 공연 큐레이션 앱이라 해당사항 없음 | 다른 프로젝트 문구가 섞여 들어온 것으로 보임 |

### 7-3. 제안

- 첨부해주신 **이미지 3장(4·5·6번) 기준으로는 이미 구현·검증이 끝난 상태**이므로, 별도 확인이 없다면 이 부분은 **현재 구조를 그대로 유지**하겠습니다.
- 다만 위 표의 "시놉시스/출연진/보관함 담기" 내용은 다음 작업 대상인 **공연 상세화면(detail.js)** 쪽 요구사항으로 보이니, 상세화면 작업 시 반영하겠습니다.
- 파일을 정말로 `favorite_search.html` / `favorite_arist.html` / `complete.html` 3개로 분리하고 싶으신 경우, 지금처럼 이미지 기준으로 재현하되 **파일만 3개로 나누는 리팩터링**을 진행할 수 있습니다 (내용은 동일, 구조만 분리).
- 하단 탭바 라벨("홈,탐색,캘린더,분석,프로필" vs "캘린더,보관함,리포트,프로필")은 어느 쪽이 최신 기준인지 확인 부탁드립니다.

확인해주시는 대로 다음 화면(상세화면 `detail.js`) 작업을 이어가겠습니다.


---

## 8. 추가 요청 반영 — 공연 상세화면 / 캘린더 / 알람설정 (main_detail.html · calendar.html · alarm.html)

이번에 요청하신 3장(메인상세정보=8번 이미지, 캘린더=9번 이미지, 티켓오픈알람=10번 이미지)은 이미 위 **4-5, 4-6, 4-7번 항목**에서 계획을 세워둔 화면과 동일합니다. 파일명만 `main_detail.html`(기존 계획엔 `detail.html`)로 바꿔서 진행할게요. 다만 지난번과 마찬가지로, 함께 주신 텍스트 요구사항 중 일부가 실제 첨부 이미지와 달라 확인 부탁드립니다. (이번에도 코드는 작성하지 않고 계획만 정리했습니다.)

### 8-1. main_detail.html — 요청 텍스트 vs 실제 이미지(8번)

| 항목 | 요청 텍스트 | 실제 이미지(8번) |
|---|---|---|
| 화면 성격 | "공연 플레이리스트" | 특정 공연 1개의 **상세 정보 화면** (플레이리스트 아님) |
| 헤더 | 중앙 로고 + **우측 알림 아이콘** | 좌측 **뒤로가기(‹)** + 중앙 로고 (알림 아이콘 없음) |
| 타이틀 | "나만의 보관함에 담기", "**소연 님의 취향일지**" | 공연 제목 "**위키드(Wicked)**" + 그 아래 "나만의 보관함에 담기" **버튼**(타이틀 아님) |
| 레이아웃 | "하단에 공연 상세정보, 그 우측에 보관함 담기 버튼" | 보관함 버튼은 상단(히어로 바로 아래) 전체 너비 버튼이고, "공연 상세정보"는 그 아래 별도 섹션(기간/러닝타임/티켓가격) — 버튼이 나란히 붙어있지 않음 |
| 추가 구성요소(텍스트에는 없지만 이미지엔 있음) | — | 취향일치 분석 카드(취향일치 95%, 추천 이유, 장르 선호도, 인기 급상승), 시놉시스(더보기 토글), 출연진 가로 리스트 |
| 하단 탭바 | "index와 동일" | ✅ 동일 (캘린더·보관함·리포트·프로필 공통 탭바) — 이 부분은 일치 |

→ **이미지(8번) 기준으로 이미 4-5번 계획대로 진행**하겠습니다. "취향일지"라는 표현은 실제 화면엔 없어서 반영하지 않습니다.

### 8-2. calendar.html — 요청 텍스트 vs 실제 이미지(9번)

요청 텍스트("공연일정 타이틀, 중앙 티켓오픈알림, 우측 알림 아이콘, 하단 예정된 공연 위젯, 하단 탭바 index와 동일")는 실제 이미지(9번: "공연 일정" 타이틀 → 월간 캘린더 카드 → "티켓 오픈 알림" 리스트 → "예정된 공연" 리스트 → 공통 탭바)와 **거의 일치**합니다. 4-6번 계획을 그대로 진행하면 됩니다.

추가로 말씀하신 **"알람 설정한 공연은 캘린더에 표시"**는 좋은 디테일이라 반영하겠습니다 — `alarm.js`에서 알람을 설정(토글 on)하면 `calendarMarkers`에 해당 날짜의 점(dot)이 추가되고, 캘린더 화면에도 반영되도록 두 화면의 데이터를 연결하겠습니다.

### 8-3. alarm.html — 요청 텍스트 vs 실제 이미지(10번)

| 항목 | 요청 텍스트 | 실제 이미지(10번) |
|---|---|---|
| 헤더 | 뒤로가기 + "알람 설정" 타이틀 | ✅ 일치 |
| 대상 공연 카드 | 포스터 썸네일 + 공연명 + 일시 | ✅ 일치 (태그 2개: 뮤지컬/단독판매 도 포함) |
| 알람 토글 리스트 | 24시간 전/10시간 전/1시간 전, 활성 항목은 메인컬러 | ✅ 일치 |
| **하단 네비게이션 탭 바** | "index와 동일" (탭바 있음) | ❌ **탭바 없음** — 대신 하단에 "설정 완료" 버튼만 있음 |

→ 이미지 기준으로는 알람설정 화면에 **하단 탭바가 없고 "설정 완료" 버튼만** 있습니다. 탭바를 추가하고 싶으신 별도 의도가 있으시면 말씀해주시고, 없으면 **이미지 기준(탭바 없음)으로 진행**하겠습니다.

### 8-4. 기타

- "습관 목록은 배열로 관리해줘"라는 문구가 이번에도 포함되어 있는데, 지난번과 동일하게 NOLI(공연 큐레이션 앱)에는 해당사항이 없어 보입니다. 혹시 매번 사용하시는 요청 템플릿에 고정 문구로 들어있는 거라면 무시하고 진행하겠습니다 (모든 데이터는 계획대로 id 기반 배열/객체로 관리합니다).

### 8-5. 작업 순서

말씀하신 순서 그대로 반영합니다.

1. 공통 스타일/컴포넌트 보강 확인 (이미 3-4번 화면 작업 때 `common.css`/`common.js`에 상당 부분 준비해둠 — 상세/캘린더/알람에 필요한 추가 토큰만 보강)
2. **공연 상세화면** (`main_detail.html`)
3. **캘린더** (`calendar.html`)
4. **알람설정** (`alarm.html`)

확인해주시면 순서대로 `main_detail.html`부터 진행하겠습니다.


---

## 9. 추가 요청 반영 — 보관함 / 리포트 / 프로필 (playlist.html · report.html · mypage.html)

이번 3장(11번=보관함, 12번=리포트, 13번=프로필)은 위 **4-8, 4-9, 4-10번 항목**에서 이미 계획을 세워둔 화면과 동일합니다. 파일명만 요청하신 대로 `playlist.html`(기존 계획엔 `storage.html`) / `report.html`(동일) / `mypage.html`(기존 계획엔 `profile.html`)로 바꿔서 진행하겠습니다. 데이터도 `savedShows`, `recentlyViewed`, `viewingTrend`, `genreDistribution`, `topArtists`, `memoryList`, `profileMenu`가 이미 `data.js`에 준비되어 있어서 바로 화면 작업이 가능합니다.

지난 두 번과 다르게 이번엔 텍스트 요구사항이 실제 이미지와 **대부분 일치**합니다. 확인이 필요한 부분만 짚어드릴게요.

### 9-1. playlist.html (보관함, 11번 이미지)
요청 내용("중앙 로고 헤더 + 나의 공연 보관함 카드 + 최근 본 공연 카드")이 이미지와 일치합니다. 그대로 진행하겠습니다.

### 9-2. report.html (리포트, 12번 이미지)
요청 내용("6개월 통계 그래프 + 장르별 원형 그래프 + 최애 아티스트 랭킹 박스 + 공유하기 버튼")이 이미지와 정확히 일치합니다. 그대로 진행하겠습니다.

### 9-3. mypage.html (프로필, 13번 이미지) — 확인 필요 1가지

말씀하신 "프로필 우측 나의보관함 아이콘 클릭하면 playlist페이지로 이동"에서, 실제 13번 이미지의 우측 상단 아이콘은 **돋보기(검색) 모양**입니다 (보관함/북마크 아이콘이 아님).

→ 이렇게 이해하고 진행하겠습니다: **아이콘 모양은 이미지 그대로 돋보기로 두되, 클릭했을 때의 동작만 말씀하신 대로 보관함(`playlist.html`) 화면으로 이동**하도록 연결합니다. (모양까지 보관함 아이콘으로 바꾸는 것을 원하시면 말씀해주세요.)

그 외 "상단 헤더: 뒤로가기 버튼" 등은 이미지와 일치하며, 다른 화면들과 동일하게 공통 헤더/하단 탭바 컴포넌트를 재사용합니다.

### 9-4. 기타
- "습관 목록은 배열로 관리해줘" 문구는 이번에도 포함되어 있는데, 지난 두 번과 동일하게 참고만 하고 실제로는 공연/취향 관련 데이터를 배열로 관리하는 기존 방식을 유지하겠습니다.

### 9-5. 작업 순서

말씀하신 순서 그대로 반영합니다.

1. 공통 스타일 확인 (이미 대부분의 카드/그리드/아바타 컴포넌트가 `common.css`에 준비되어 있어 추가 보강은 적을 예정)
2. **보관함** (`playlist.html`)
3. **리포트** (`report.html`)
4. **프로필** (`mypage.html`)

확인해주시면 순서대로 `playlist.html`부터 진행하겠습니다.
