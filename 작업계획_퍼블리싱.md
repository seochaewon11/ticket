# NOLI 공연 큐레이션 앱 — 퍼블리싱 작업계획

## 0. 프로젝트 개요
- **앱명**: NOLI (지금 즐기는 나만의 공연)
- **목적**: 사용자 취향 기반 공연 추천 + 관람 리스트 관리
- **화면 수**: 3개 (splash / index / add)
- **기술스택**: Vanilla HTML / CSS / JS (프레임워크 없음, 추후 React 전환 고려한 데이터 구조)
- **디자인 톤**: 보라 계열(#A78BFA) + 아이보리 포인트(#F7F0EC), 미니멀 카드 UI
- **반응형 기준**: max-width 480px, 모바일 우선

---

## 1. 화면 전환 방식에 대한 가정 (⚠️ 확인 필요)

요구사항에 "페이지 이동 없이 JS로 SPA 방식 처리"라고 되어 있으나, 동시에 `splash.html`, `index.html`, `add.html` **3개의 개별 HTML 파일**로 구성하라고 명시되어 있어 두 조건이 상충합니다. 아래와 같이 해석하고 진행할 예정이며, 다른 의도이시면 알려주세요.

- 3개 파일은 **별도의 HTML 문서**로 유지 (요청하신 파일 구조 우선)
- 화면 "전환"은 클릭 후 새로고침되는 전통적 하이퍼링크가 아니라, **JS가 제어하는 자동/버튼 트리거 이동**으로 구현
  - splash → index : `setTimeout` 이후 JS가 `location.href` 이동 (사용자 클릭 불필요, 로딩 후 자동 전환)
  - index ↔ add : 버튼 클릭 시 JS 이벤트 핸들러로 이동 처리 (단, 데이터는 새로고침에도 유지되도록 `localStorage`에 저장)
- 화면 간 데이터 공유는 `data.js`의 배열 구조를 **localStorage에 동기화**하여, add.html에서 저장한 취향/공연 데이터가 index.html 진입 시 즉시 반영되도록 처리
- 추후 React 전환 시에는 이 3개 화면이 각각 라우트(컴포넌트)가 되고, `data.js`의 배열 구조가 그대로 전역 상태(Context/Store) 초기값으로 이식 가능하도록 설계

---

## 2. 디자인 분석 (화면별 요소 정리)

### 2-1. splash.html (스플래시)
- 배경: 보라 → 아이보리 방향 그라디언트 (풀스크린, 모서리에 blur된 광원 느낌)
- 중앙 흰 원 안에 보라색 원형 로고, 로고 텍스트 "NOLI"
- 로고 아래 슬로건: "지금 즐기는 나만의 공연"
- 하단: 좌측부터 채워지는 얇은 progress bar (보라색)
- progress bar 아래 캡션: "INITIALIZING EXPERIENCE" (영문 대문자, 자간 넓게, 회색톤)
- 동작: 진입 시 progress bar 애니메이션 실행 → 완료 후 자동으로 index.html 이동

### 2-2. index.html (메인 홈)
- **상단 헤더**: 좌측 프로필 원형 아이콘 / 중앙 "NOLI" 로고 텍스트 / 우측 설정(gear) 아이콘
- **타이틀 영역**: "오늘의 추천" + 서브텍스트 "나의 감성을 채워줄 오늘의 무대"
- **추천 섹션 헤더**: "OO님 취향 추천" (좌) + "더보기 >" 링크(우)
- **추천 카드 (핵심 컴포넌트)**
  - 포스터 이미지 (카드 상단, 라운드 처리)
  - 우측 상단 좋아요(하트) 토글 버튼 — 클릭 시 채움/빈 상태 전환
  - 이미지 하단 배지 2종: 일치율 뱃지("내 취향 일치 94%", 보라 배경) + 장르 뱃지("뮤지컬", 반투명 흰 배경)
  - 공연명, 날짜/기간, 장소 텍스트
  - CTA 버튼 1: "공연보관함에 담기" (보라 채움, filled)
  - CTA 버튼 2: "공연 상세 정보 보기" (연회색 비활성 톤, outline/disabled 느낌)
- **나의 공연 플레이리스트 섹션**
  - 섹션 타이틀: "나의 공연 플레이리스트"
  - 리스트 아이템 카드 2개 (흰 배경, 라운드, 그림자)
    - 아이콘(원형 배경) + 라벨("보고 싶은 공연" / "관람 완료 공연") + 수치(굵게, 큰 폰트) + 우측 화살표(>)
    - 보고싶은 공연: 보라 아이콘 배경 + 하트 아이콘
    - 관람완료 공연: 핑크 아이콘 배경 + 티켓 아이콘
  - 수치는 데이터 배열에서 실시간 count로 계산되어 표시
- **하단 탭바 (Bottom Navigation)**: Today(활성, 보라 pill 강조) / My List / Archive / Profile — 4개 아이콘+라벨

### 2-3. add.html (취향설정)
- **상단바**: 좌측 back arrow, 중앙 원형 NOLI 미니 로고
- **진행 표시**: 3단계 progress bar (세그먼트 3개, 현재 2번째까지 채워진 형태로 시안에 표현) + "Step 1 of 3" 텍스트 + 우측 "관심 아티스트" 링크
- **타이틀**: "어떤 공연을 좋아하세요?" + 서브텍스트 "취향에 맞는 공연을 추천해 드릴게요."
- **장르 선택 카드 그리드 (2열 x 3행)**
  - 콘서트 / 뮤지컬 / 밴드 / 댄스 / 클래식 / 전시
  - 각 카드: 원형 아이콘(색상 다르게: 보라/핑크/그레이/보라/그레이/핑크 톤) + 라벨 텍스트
  - 카드 클릭 시 다중 선택 가능 (선택 상태 시 보더/배경 하이라이트로 시각 피드백 필요 — 시안에는 미선택 상태만 있어 선택 시 스타일은 보라 보더+연보라 배경으로 별도 정의)
- **분위기 태그 선택**: "좋아하는 분위기는?" 타이틀 + pill 형태 태그 4개 (에너지 넘치는 / 감성적인 / 유쾌한 / 몰입되는), 다중 선택 가능, 선택 시 보라 채움으로 전환
- **하단 CTA 버튼**: "다음 단계로 →" (보라 채움, 풀와이드, 화면 하단 고정)
- 동작: 버튼 클릭 시 선택된 장르+분위기 데이터를 취향 객체로 만들어 `data.js` 배열에 저장(localStorage 동기화) → index.html의 추천 리스트에 즉시 반영되도록 필터/가중치 로직 연결

---

## 3. 파일 구조

```
noli/
├── splash.html
├── index.html
├── add.html
├── css/
│   ├── common.css        # 컬러/폰트 변수, reset, 공통 컴포넌트(버튼, 배지, 카드 등)
│   ├── splash.css
│   ├── index.css
│   └── add.css
├── js/
│   ├── data.js            # 공통 데이터 정의 및 localStorage 연동 로직 (공연 배열, 유저 취향 배열)
│   ├── splash.js
│   ├── index.js
│   └── add.js
└── assets/
    ├── images/             # 포스터, 로고 등
    └── icons/              # svg 아이콘 (하트, 티켓, 장르 아이콘 등)
```

---

## 4. 데이터 구조 설계 (`data.js`)

> React 전환을 고려해 **평탄한 배열(Array) + 명시적 필드**로 설계하고, 모든 변경은 순수 함수 형태(add/update/toggle)로 분리합니다.

### 4-1. 공연 데이터 배열 `performances`
```js
// 필드 예시 (실제 코드는 이후 단계에서 작성)
{
  id: "perf_001",
  title: "뮤지컬 위키드",
  genre: "뮤지컬",              // 콘서트 | 뮤지컬 | 밴드 | 댄스 | 클래식 | 전시
  mood: ["몰입되는", "감성적인"], // 취향 매칭에 사용
  matchRate: 94,                // 취향 일치율(%) — 유저 취향과 비교해 산출
  posterUrl: "assets/images/wicked.jpg",
  period: "2026.09.15 - 10.20",
  venue: "블루스퀘어 신한카드홀",
  liked: false,                 // 하트 토글 상태
  status: null                  // null | "wish"(보고싶은 공연) | "watched"(관람완료)
}
```

### 4-2. 유저 취향 데이터 `userPreference`
```js
{
  favoriteGenres: [],   // ["뮤지컬", "콘서트"] 등 add.html에서 선택
  favoriteMoods: [],    // ["에너지 넘치는", "몰입되는"] 등
  updatedAt: "ISO date"
}
```

### 4-3. 파생 데이터 (계산값, 별도 저장 X)
- `wishCount` = performances.filter(status === 'wish').length → "보고 싶은 공연" 수치
- `watchedCount` = performances.filter(status === 'watched').length → "관람 완료 공연" 수치
- 추천 리스트 = performances를 userPreference 기준(genre/mood 매칭 + matchRate)으로 정렬/필터

### 4-4. 영속성
- 최초 로드시 `data.js` 내 초기 목데이터(dummy performances 3~5건)를 기본값으로 세팅
- 이후 모든 변경(좋아요 토글, 보관함 담기, 취향설정 저장)은 `localStorage`에 반영 → 페이지 이동/새로고침에도 유지
- `data.js`에 `getData()`, `saveData()`, `updatePreference()`, `toggleLike(id)`, `addToWishlist(id)` 등 공통 함수만 이 단계에서 이름/역할을 정의 (실제 구현은 코드 작성 단계에서)

---

## 5. 단계별 작업 순서

### Step 1. 공통 스타일 정의 (`common.css`)
- CSS 변수 정의: `--color-primary(#A78BFA)`, `--color-primary-dark`, `--color-accent(#F7F0EC)`, `--color-text`, `--color-text-sub`, `--radius-*`, `--shadow-*`, `--font-family`
- 리셋 CSS (box-sizing, margin/padding 초기화, 버튼/인풋 기본 스타일 제거)
- 공통 컴포넌트 클래스 사전 정의: `.btn-primary`, `.btn-disabled`, `.badge`, `.card`, `.bottom-nav` 등 (화면별 css에서 재사용)
- 모바일 컨테이너 기준 (`max-width: 480px`, 중앙 정렬) 레이아웃 wrapper 정의

### Step 2. 메인 화면 (`index.html` / `index.css` / `index.js`)
1. 헤더 + 추천 타이틀 영역 마크업
2. 추천 카드 컴포넌트 마크업 및 스타일 (포스터, 뱃지 2종, 하트 토글 버튼)
3. `data.js`의 `performances` 배열을 읽어 추천 카드를 동적 렌더링 (최소 1건 시안 기준 먼저 구현 후 리스트 확장)
4. 좋아요 토글 인터랙션(JS) — 클릭 시 상태 반전 + localStorage 반영
5. "공연보관함에 담기" 버튼 클릭 시 해당 공연 status를 'wish'로 변경
6. 나의 공연 플레이리스트 섹션 — wishCount/watchedCount 실시간 계산 및 렌더링
7. 하단 탭바 마크업(Today 활성 상태 스타일만 우선, 탭 전환 기능은 범위 외로 두고 UI만 구현)

### Step 3. 스플래시 화면 (`splash.html` / `splash.css` / `splash.js`)
1. 그라디언트 배경 + 원형 로고 + 슬로건 마크업/스타일
2. progress bar CSS 애니메이션 (일정 시간 동안 0→100% width 채워짐)
3. JS: 진입 시 타이머 시작 → 애니메이션 종료 시점에 `index.html`로 자동 이동

### Step 4. 취향설정 화면 (`add.html` / `add.css` / `add.js`)
1. 상단바(back arrow, 로고) + 3단계 progress 인디케이터 + "Step 1 of 3" 마크업
2. 장르 선택 카드 그리드(2x3) 마크업 및 스타일, 다중 선택 토글(선택 시 하이라이트 스타일 추가 정의)
3. 분위기 태그 pill 다중 선택 토글 마크업/스타일
4. "다음 단계로" 버튼 클릭 이벤트:
   - 선택된 장르/분위기를 `userPreference` 객체로 구성 → `data.js` 통해 저장
   - 저장 후 `index.html`로 이동 (또는 이동 전 안내 후 이동)
5. index.html 재진입 시 저장된 취향 기준으로 추천 리스트 매칭 로직 반영 확인

---

## 6. 화면별 작업 요청 시 참고할 체크리스트
작업 순서대로 요청 주시면, 각 단계에서 아래를 함께 확인하며 진행하겠습니다.
- [ ] 마크업 구조 (semantic tag 사용)
- [ ] common.css 변수 재사용 여부
- [ ] 반응형(480px 기준) 정렬
- [ ] 인터랙션 동작 확인 (토글, 저장, 화면 이동)
- [ ] data.js 연동 및 localStorage 반영 확인

---

## 7. 확인 요청 사항 정리
1. 위 "화면 전환 방식 해석"(섹션 1)대로 진행해도 괜찮을까요?
2. index.html의 추천 카드가 현재 시안처럼 1건만 노출되는 구조인지, 스크롤/캐러셀로 여러 건이 노출되어야 하는지 확인 필요합니다. ("더보기 >" 링크가 있어 다건 데이터를 가정하고 설계했습니다)
3. 하단 탭바의 My List / Archive / Profile 탭은 이번 3개 화면 범위에 없으므로, UI만 구현하고 클릭 동작(페이지 이동)은 범위 외로 처리해도 괜찮을지 확인 필요합니다.
4. 포스터 이미지 등 실제 에셋이 없는 경우 플레이스홀더 이미지로 대체할 예정입니다.

---

## 8. 참고: 이전 작업물 안내
이전 대화에서 이미 이 계획을 기반으로 메인 화면(`index.html`, `css/common.css`, `css/index.css`, `js/data.js`, `js/index.js`)까지 작업이 완료되어 전달된 적이 있습니다. 이번 계획서는 요청하신 대로 처음부터 다시 정리한 버전이며, 실제 코드 작업을 다시 진행할지, 기존 결과물을 이어서 쓸지는 다음 단계 요청 시 말씀해 주시면 그에 맞게 진행하겠습니다.

---

## 9. 신규 화면 확장 계획 (공연 상세 / 관심 아티스트 / 설정완료)

기존 3화면(splash / index / add) 계획에 이어, 첨부해주신 시안 3장을 기반으로 아래 3개 화면을 추가합니다. 공통 스타일(`common.css`)은 기존 것을 그대로 재사용하고, 화면별 css/js만 새로 추가합니다.

### 9-1. detail.html (공연 상세 정보)
- **상단 내비게이션 바**: 뒤로가기(back arrow, 좌측) / 공유(share) 아이콘 + 알림(bell) 아이콘(우측, 그룹) — 반투명 배경 위에 떠 있는 형태
- **비주얼 영역**
  - 포스터 이미지 전체 폭 노출 (`<img>` 태그, 카드 아님 — 화면 상단에 바로 배치)
  - 이미지 위 `position: absolute` 배지 2종
    - "현재 상영 중" — 이미지 상단부, 반투명 흰 배경 pill
    - "❤ 내 취향 일치 92%" — 이미지 하단, 보라 채움 pill (하트 아이콘 포함)
  - 이미지 하단(카드 영역 시작 지점)에 공연명 "위키드 (Wicked)" 대형 타이틀 + 서브카피(영문 태그라인)
  - 장르/지역/공연장 태그 칩 3개 가로 나열 (연핑크 배경 pill): 뮤지컬 · 서울 · 샤롯데씨어터
- **추천 이유 툴팁 박스** (흰 카드, 둥근 모서리)
  - 상단: ✨ 아이콘 + "왜 추천됐는지 궁금하시죠?" 타이틀
  - 사용자 맞춤 안내 문장 (취향 태그를 문장에 삽입하는 동적 텍스트 — 예: "평소 판타지 장르와 여성 중심 서사를 즐기시는군요!")
  - 가로형 미니 카드 2개
    - 장르 선호도 카드: 아이콘 + "장르 선호도" 라벨 + "판타지 뮤지컬 카테고리 1위" 값
    - 인기 급상승 카드: 아이콘 + "인기 급상승" 라벨 + "최근 24시간 내 2,580명 찜" 값
- **공연 상세 정보 섹션** (섹션 타이틀 + 우측 "더보기" 링크)
  - `flex` 레이아웃의 정보 행 3개 (아이콘 + 라벨 + 값)
    - 기간 (calendar 아이콘): "2024.10.05 ~ 2025.01.15"
    - 러닝타임 (clock 아이콘): "170분 (인터미션 20분 포함)"
    - 티켓 가격 (ticket 아이콘): "VIP 170,000원 ~ B석 70,000원"
- **시놉시스**: 연핑크 배경 카드에 본문 텍스트 + 하단 "더 보기 ⌄" 토글 (클릭 시 텍스트 펼침/접힘, JS로 구현)
- **출연진**: 섹션 타이틀 + 우측 "전체보기" 링크, 가로 스크롤 리스트
  - 각 아이템: 원형 프로필 사진(`<img>`) + 배우명(굵게) + 배역명(회색 작은 글씨)
  - 프로필 사진이 없는 경우 회색 원형 placeholder 아이콘으로 대체 (시안 속 "고은성" 항목 참고)
- **하단 고정 액션바**
  - "이전" 버튼(아웃라인/연회색) + "공연보관함에 담기" 버튼(보라 채움, + 아이콘) 2개를 가로로 배치
  - 그 아래 공통 하단 탭바: 홈 / 탐색 / 캘린더 / 분석 / 프로필 (아이콘 5개)

> ⚠️ **확인 필요**: 이 화면의 하단 탭바 구성(홈·탐색·캘린더·분석·프로필, 5개)이 기존 index.html의 하단 탭바(Today·My List·Archive·Profile, 4개)와 다릅니다. 두 화면이 서로 다른 탭 구성을 각자 유지하는 것으로 보고 시안 그대로 각각 구현할 예정이며, 만약 두 화면이 하나의 앱 안에서 동일한 탭바를 공유해야 한다면 알려주세요.

### 9-2. artist.html (관심 아티스트 선택)
- **상단**: 뒤로가기 버튼(좌측 상단) + 진행률 바(Progress Bar, 약 50% 채움) + 그 아래 "Step 2 of 3" 텍스트
- **타이틀 영역**: "좋아하는 아티스트가 있나요?" (2줄) + 서브 안내 "선택하신 아티스트의 공연 소식을 가장 먼저 알려드릴게요."
- **검색바**: 둥근 pill 형태, 좌측 돋보기 아이콘 + `<input type="text">` (placeholder: "아티스트 이름을 검색해보세요")
- **아티스트 그리드**: 3열 그리드, 원형 프로필 사진 + 이름(중앙 정렬)
  - 다중 선택 가능하도록 구현 (시안에는 1명만 선택된 상태가 보이지만, 취향설정 화면의 장르 선택과 일관성 있게 다중 선택으로 진행 — 단일 선택이 맞다면 말씀해 주세요)
  - 선택된 카드: 원형 사진에 보라 테두리(ring) + 우측 하단에 보라 원형 체크 배지 오버레이 (`is-selected` 클래스)
- **하단 고정 버튼**: "설정 완료" 풀와이드 버튼
  - 기본 상태: 비활성(연회색, disabled)
  - 아티스트 1명 이상 선택 시 JS로 활성화(보라 채움) 전환
  - 클릭 시 선택한 아티스트 목록을 `userPreference.favoriteArtistIds`에 저장 → complete.html로 이동

### 9-3. complete.html (설정완료)
- **배경 연출**: 파스텔톤(연보라/연핑크) 도트 파티클을 화면 전체에 랜덤 배치 (크기·투명도 다르게, 가벼운 인라인 SVG 또는 CSS `::before/::after` + `box-shadow` 다중 점으로 구현 — 외부 라이브러리 없이 순수 CSS/SVG)
- **완료 메시지**: 중앙 정렬, "설정이 완료되었습니다!" (굵고 크게) + "이제 NOLI가 소연님의 취향을 찾았어요." (서브텍스트)
- **TASTE ANALYSIS 카드** (흰 카드, 둥근 모서리, 그림자)
  - 라벨: "TASTE ANALYSIS" (보라, 자간 넓은 소문자 캡션 스타일)
  - 타이틀: "소연님을 위한 맞춤 공연을 준비했어요" (굵게)
  - 해시태그 pill 2x2 그리드: #감성적인 · #뮤지컬덕후 · #라이브공연 · #취향저격 (앞서 add.html에서 선택한 `favoriteMoods`/장르 기반으로 자동 생성되도록 구성 — 값이 없을 경우 기본 목데이터 사용)
  - 안내 문구: "소연 님이 좋아할 만한 공연 정보들을 매일 아침 배달해 드릴게요! 💌"
- **하단 CTA 버튼**: "NOLI 시작하기 →" 풀와이드 보라 버튼, 클릭 시 JS로 `index.html` 이동

---

## 10. 신규 화면 반영 — 파일 구조 업데이트

```
noli/
├── splash.html
├── index.html
├── add.html
├── detail.html            # 신규
├── artist.html             # 신규
├── complete.html           # 신규
├── css/
│   ├── common.css
│   ├── splash.css
│   ├── index.css
│   ├── add.css
│   ├── detail.css          # 신규
│   ├── artist.css          # 신규
│   └── complete.css        # 신규
├── js/
│   ├── data.js              # 아티스트/추천이유/출연진 데이터 필드 추가
│   ├── splash.js
│   ├── index.js
│   ├── add.js
│   ├── detail.js            # 신규
│   ├── artist.js            # 신규
│   └── complete.js          # 신규
└── assets/
    ├── images/               # 포스터, 배우 프로필 사진 등
    └── icons/
```

---

## 11. 신규 화면 반영 — 데이터 구조 확장 (`data.js`)

기존 `performances` 배열에 상세 화면용 필드를 추가하고, 아티스트 배열/취향 필드를 새로 정의합니다.

### 11-1. `performances` 배열 필드 확장
```js
{
  id: "perf_001",
  title: "뮤지컬 위키드",
  genre: "뮤지컬",
  mood: ["몰입되는", "감성적인"],
  matchRate: 94,
  posterUrl: "assets/images/wicked.jpg",
  period: "2026.09.15 - 10.20",
  venue: "블루스퀘어 신한카드홀",
  liked: false,
  status: null,

  // ---- 아래부터 상세 화면(detail.html)용 신규 필드 ----
  isRunning: true,                 // "현재 상영 중" 배지 노출 여부
  tagline: "BROADWAY'S BIGGEST BLOCKBUSTER",
  tags: ["뮤지컬", "서울", "샤롯데씨어터"],
  recommendReason: {
    summary: "평소 판타지 장르와 여성 중심 서사를 즐기시는군요! ...",
    genrePreferenceLabel: "판타지 뮤지컬 카테고리 1위",
    trendingLabel: "최근 24시간 내 2,580명 찜"
  },
  runtime: "170분 (인터미션 20분 포함)",
  priceRange: "VIP 170,000원 ~ B석 70,000원",
  synopsis: "도로시가 오즈에 떨어지기 훨씬 전, ...",
  cast: [
    { name: "옥주현", role: "엘파바 역", photoUrl: "assets/images/cast/ok.jpg" },
    { name: "정선아", role: "글린다 역", photoUrl: "assets/images/cast/jung.jpg" },
    { name: "고은성", role: "피에로 역", photoUrl: null } // null이면 회색 placeholder 아이콘 표시
  ]
}
```

### 11-2. 아티스트 데이터 배열 `artists` (신규)
```js
{
  id: "artist_001",
  name: "홍광호",
  photoUrl: "assets/images/artists/hong.jpg"
}
```

### 11-3. `userPreference` 필드 확장
```js
{
  favoriteGenres: [],
  favoriteMoods: [],
  favoriteArtistIds: [],   // artist.html에서 선택한 artists의 id 배열 (신규)
  updatedAt: "ISO date"
}
```

### 11-4. 파생 데이터 (complete.html 해시태그 생성용)
- 완료 화면의 `#감성적인 #뮤지컬덕후 #라이브공연 #취향저격` 같은 해시태그는 `userPreference.favoriteMoods` + `favoriteGenres`를 조합해 동적으로 생성
- 예: mood "감성적인" → `#감성적인` 그대로, genre "뮤지컬" → `#뮤지컬덕후`처럼 매핑 테이블(`js/data.js` 내 `MOOD_TAG_MAP`, `GENRE_TAG_MAP`)을 둬서 변환

---

## 12. 신규 화면 반영 — 단계별 작업 순서 (요청하신 순서대로)

기존 계획(섹션 5)의 "Step 1. 공통 스타일"은 이미 완료된 `common.css`를 그대로 재사용하므로 생략하고, 아래 순서로 이어서 진행합니다.

### Step 5. 공연 상세 화면 (`detail.html` / `detail.css` / `detail.js`)
1. 상단 내비게이션 바(뒤로가기 / 공유 / 알림) 마크업
2. 비주얼 영역 — 포스터 `<img>` + absolute 배지 2종("현재 상영 중", "내 취향 일치 92%") + 태그 칩 3개
3. 추천 이유 툴팁 박스 마크업 및 스타일, `data.js`의 `recommendReason` 필드를 읽어 동적 렌더링
4. 공연 상세 정보(`flex` 정보 행 3개: 기간/러닝타임/가격) 마크업
5. 시놉시스 카드 + "더 보기" 토글 인터랙션(JS, 텍스트 펼침/접힘)
6. 출연진 가로 스크롤 리스트 렌더링 (`cast` 배열 기반, 사진 없는 경우 placeholder 처리)
7. 하단 고정 액션바("이전"/"공연보관함에 담기") + 하단 탭바(홈/탐색/캘린더/분석/프로필) 마크업
8. "공연보관함에 담기" 클릭 시 해당 공연 `status`를 `'wish'`로 변경(기존 `NoliData.addToWishlist` 재사용)

### Step 6. 관심 아티스트 선택 화면 (`artist.html` / `artist.css` / `artist.js`)
1. 상단 뒤로가기 + progress bar(Step 2 of 3 표시) 마크업
2. 타이틀/서브텍스트 + 검색바(`<input>`) 마크업, 검색어 입력 시 아티스트 목록 필터링(JS)
3. 3열 아티스트 그리드 렌더링(`artists` 배열 기반), 클릭 시 다중 선택 토글(`is-selected` 클래스: 보라 테두리 + 체크 배지)
4. 하단 "설정 완료" 버튼 — 선택된 아티스트 0명일 땐 비활성, 1명 이상이면 활성화되는 JS 인터랙션
5. 클릭 시 `favoriteArtistIds` 저장 → `complete.html`로 이동

### Step 7. 설정완료 화면 (`complete.html` / `complete.css` / `complete.js`)
1. 배경 도트 파티클 연출(CSS/SVG) 마크업
2. 완료 메시지 영역 마크업
3. TASTE ANALYSIS 카드 — 저장된 `userPreference` 기반 해시태그 동적 렌더링
4. 안내 문구 영역 마크업
5. "NOLI 시작하기" 버튼 클릭 시 `index.html`로 이동(JS)

---

## 13. 신규 화면 확인 요청 사항 정리
1. detail.html과 index.html의 하단 탭바 구성이 서로 다른데(9-1 참고), 각 화면 시안 그대로 유지해도 괜찮을까요?
2. artist.html의 아티스트 선택을 다중 선택으로 구현할 예정인데, 시안처럼 단일 선택이 맞다면 알려주세요.
3. detail.html의 출연진 사진, artist.html의 아티스트 사진은 실제 에셋이 없으므로 플레이스홀더(회색 원형 아이콘)로 대체할 예정입니다.
4. complete.html의 해시태그는 실제 선택한 취향(mood/genre) 기반으로 동적 생성할지, 시안처럼 고정 문구로 둘지 확인 부탁드립니다 (계획서에는 동적 생성으로 설계했습니다).

---

## 14. 신규 화면 확장 계획 (공연 플레이리스트 / 공연일정 / 티켓오픈알람)

이번 요청에는 시안 이미지가 첨부되지 않아, 말씀해주신 텍스트 설명만으로 계획을 세웠습니다. 실제 코드 작업 시 세부 여백/컬러/비율은 지금까지의 디자인 톤(보라 `#A78BFA` 계열 + 아이보리 `#F7F0EC`, 카드형 UI)에 맞춰 진행하고, 이미지가 있으면 그에 맞게 조정하겠습니다.

공통 스타일(`common.css`)은 기존 것을 그대로 재사용하고, 화면별 css/js만 새로 추가합니다.

### 14-1. play.html (공연 플레이리스트)
- **상단 헤더**: 좌측 햄버거 메뉴 아이콘 / 중앙 "NOLI" 로고 / 우측 알림(bell) 아이콘
- **타이틀 영역**: "나의 공연 보관함" + 서브 문구 "소중한 추억과 보고 싶은 무대들을 모았어요 ✨"
- **플레이리스트 카드 리스트** (세로 카드, 배열 데이터 기반 반복 렌더링)
  - 카드 배경: 공연 관련 이미지(또는 그라디언트 placeholder, 기존 인덱스 화면의 장르별 그라디언트 로직 재사용)
  - 카드 내부: 타이틀(예: "보고 싶은 뮤지컬", "올해 꼭 갈 콘서트") + 하단 태그라인(설명 문구)
  - 우측 상단(또는 하단): 참여자 아바타 아이콘(원형, 겹쳐진 스택 형태) + 공유 버튼 + "담기" 버튼
- **최근 본 공연 추천 섹션**: "최근 본 공연 추천 🎨" 타이틀 + 우측 "모두 보기" 링크, 가로 스크롤 썸네일 카드 목록 (기존 `cast-scroll`과 동일한 가로 스크롤 패턴 재사용)
- **하단 탭바**: index.html과 동일 구성(Today / My List / Archive / Profile) 그대로 재사용

> ⚠️ **확인 필요**: "담기" 버튼의 정확한 동작(플레이리스트를 내 보관함에 저장 vs 플레이리스트 안의 공연을 개별적으로 담기)이 시안 없이는 명확하지 않아, 우선 "이 플레이리스트 전체를 저장" 동작으로 가정하고 진행합니다.

### 14-2. calendar.html (공연 일정)
- **상단 헤더**: 좌측 사용자 프로필 아바타(원형) / 중앙 "NOLI" 로고 / 우측 알림 아이콘
- **타이틀 영역**: "공연 일정" + 서브 텍스트 "이번 달의 예술적 여정을 확인하세요."
- **캘린더 위젯 카드**
  - 상단: 이전/다음 달 이동 버튼(‹ ›) + "2026년 10월" 타이틀
  - 요일 헤더(일~토) + 날짜 그리드(6주 x 7일)
  - 선택된 날짜(15일)는 보라색 원형 배경으로 강조
  - 공연 일정이 있는 날짜 하단에 작은 점(dot) 표시
  - 날짜 클릭 시 선택 상태 및 하단 "예정된 공연" 리스트 필터링(JS)
- **티켓 오픈 알람 섹션**: "티켓 오픈 알람" 타이틀 + 우측 "모두 보기", 가로 스크롤 카드(예: "D-3", "뮤지컬 오페라의 유령") — 카드 클릭 시 `alarm.html`로 이동
- **예정된 공연 섹션**: 날짜 + 장소가 포함된 세로 리스트 카드(예: "서울시립교향악단 정기공연", "가을 밤의 어쿠스틱")
- **하단 탭바**: index.html과 동일 구성 재사용

### 14-3. alarm.html (티켓오픈알람)
- **상단 헤더**: 뒤로가기(‹) + "알람 설정" 타이틀 (중앙 정렬)
- **대상 공연 정보 카드**
  - 포스터 썸네일(작은 정사각/세로형 이미지)
  - 장르 태그 2개(예: "뮤지컬", "단독판매")
  - 공연명 + 공연 일시 정보
- **알람 시간 선택 리스트**
  - 안내 문구: "원하는 알람 시간을 모두 선택해주세요" + 서브 텍스트
  - 항목: 24시간 전 / 1시간 전 / 30분 전 / 10분 전 (각 행에 `<input type="checkbox" role="switch">` 기반 토글 스위치)
  - 활성화된 토글은 보라색 채움으로 스타일링, 다중 선택 가능
  - 선택 상태는 `localStorage`(기존 `NoliData` 패턴)에 알람 대상 공연 id 기준으로 저장
- **하단 탭바**: index.html과 동일 구성 재사용

---

## 15. 신규 화면 반영 — 파일 구조 업데이트

```
noli/
├── splash.html
├── index.html
├── add.html
├── detail.html
├── artist.html
├── complete.html
├── play.html                # 신규
├── calendar.html            # 신규
├── alarm.html                # 신규
├── css/
│   ├── common.css
│   ├── splash.css
│   ├── index.css
│   ├── add.css
│   ├── detail.css
│   ├── artist.css
│   ├── complete.css
│   ├── play.css              # 신규
│   ├── calendar.css          # 신규
│   └── alarm.css             # 신규
├── js/
│   ├── data.js                # 플레이리스트/캘린더 이벤트/알람 데이터 필드 추가
│   ├── splash.js
│   ├── index.js
│   ├── add.js
│   ├── detail.js
│   ├── artist.js
│   ├── complete.js
│   ├── play.js                # 신규
│   ├── calendar.js            # 신규
│   └── alarm.js                # 신규
└── assets/
    ├── images/
    └── icons/
```

---

## 16. 신규 화면 반영 — 데이터 구조 확장 (`data.js`)

### 16-1. 플레이리스트 배열 `playlists` (신규)
```js
{
  id: "playlist_001",
  title: "보고 싶은 뮤지컬",
  tagline: "설렘 가득한 무대들을 모아봤어요",
  coverGradient: "뮤지컬",       // index.js의 posterGradient()에서 쓰는 장르 키 재사용
  performanceIds: ["perf_001"],  // 이 플레이리스트에 담긴 공연 id 배열
  collaboratorAvatars: ["#A78BFA", "#F0A6B8"] // 아바타 스택 표시용 임시 컬러 목데이터
}
```

### 16-2. 캘린더 이벤트 배열 `calendarEvents` (신규)
```js
{
  id: "event_001",
  performanceId: "perf_002",
  date: "2026-10-15",          // YYYY-MM-DD
  title: "내한 콘서트 Coldplay",
  venue: "고척스카이돔"
}
```

### 16-3. 티켓 오픈 알람 배열 `ticketOpenAlerts` (신규)
```js
{
  id: "alert_001",
  performanceId: "perf_005",
  title: "뮤지컬 오페라의 유령",
  openDate: "2026-10-18",
  dDay: 3,                      // "D-3" 표시용, openDate 기준 자동 계산도 가능
  genreTags: ["뮤지컬", "단독판매"],
  posterUrl: "assets/images/phantom.jpg",
  scheduleText: "2026.11.20(금) 19:30",
  reminderOptions: [
    { key: "24h", label: "24시간 전", enabled: false },
    { key: "1h", label: "1시간 전", enabled: true },
    { key: "30m", label: "30분 전", enabled: false },
    { key: "10m", label: "10분 전", enabled: false }
  ]
}
```

### 16-4. 함수 확장 (`data.js`)
- `getPlaylists()`, `getPlaylistPerformances(playlistId)` — 플레이리스트 및 소속 공연 조회
- `getCalendarEventsByMonth(year, month)`, `getCalendarEventsByDate(dateStr)` — 캘린더 렌더링/필터용
- `getTicketOpenAlerts()`, `getAlertById(id)` — 티켓 오픈 알람 목록/단건 조회
- `toggleAlertReminder(alertId, optionKey)` — alarm.html 토글 스위치 클릭 시 상태 반전 + localStorage 반영

---

## 17. 신규 화면 반영 — 단계별 작업 순서 (요청하신 순서대로)

기존과 마찬가지로 "Step 1. 공통 스타일"은 이미 완료된 `common.css`를 그대로 재사용하므로 생략합니다.

### Step 8. 공연 플레이리스트 화면 (`play.html` / `play.css` / `play.js`)
1. 상단 헤더(햄버거 메뉴 / 로고 / 알림) 마크업
2. 타이틀 영역 마크업
3. `playlists` 배열 기반 세로 카드 리스트 렌더링 (배경 그라디언트, 타이틀, 태그라인, 아바타 스택, 공유/담기 버튼)
4. "담기" 버튼 클릭 인터랙션(JS) — 클릭 시 시각적 피드백(담김 상태 토글) 처리
5. "최근 본 공연 추천" 가로 스크롤 섹션 렌더링 (`performances` 배열 중 일부 활용)
6. 하단 탭바 마크업 (index.html과 동일 마크업 재사용)

### Step 9. 공연 일정 화면 (`calendar.html` / `calendar.css` / `calendar.js`)
1. 상단 헤더(프로필 아바타 / 로고 / 알림) + 타이틀 영역 마크업
2. 캘린더 위젯 — 월/연도 상태 관리(JS), 이전/다음 달 이동 버튼, 요일·날짜 그리드 렌더링
3. 날짜 클릭 시 선택 상태(보라 원형) 갱신 + `calendarEvents` 기준 이벤트 dot 표시
4. 티켓 오픈 알람 가로 스크롤 섹션 — `ticketOpenAlerts` 렌더링, 카드 클릭 시 `alarm.html?id=` 이동
5. 예정된 공연 리스트 섹션 — 선택된 날짜(또는 이번 달 전체) 기준 `calendarEvents` 필터링해 렌더링
6. 하단 탭바 마크업(재사용)

### Step 10. 티켓오픈알람 화면 (`alarm.html` / `alarm.css` / `alarm.js`)
1. 상단 헤더(뒤로가기 + "알람 설정") 마크업
2. 대상 공연 정보 카드 — URL 쿼리(`?id=`)로 받은 `ticketOpenAlerts` 항목 렌더링, 없으면 기본값(mock 1건) 사용
3. 알람 시간 선택 리스트 — `reminderOptions` 기반 토글 스위치(`role="switch"`) 렌더링
4. 토글 클릭 시 `toggleAlertReminder()` 호출로 상태 반전 + 보라색 활성 스타일 적용(JS)
5. 하단 탭바 마크업(재사용)

---

## 18. 신규 화면(14~17) 확인 요청 사항
1. 이번엔 시안 이미지가 없어 텍스트 설명만으로 계획을 세웠습니다 — 실제 작업 시작 전에 이미지를 첨부해주시면 더 정확하게 재현할 수 있어요. 이미지 없이 텍스트 설명 기준으로 바로 진행해도 괜찮을까요?
2. play.html의 "담기" 버튼 동작(14-1 참고 — 플레이리스트 전체 저장으로 가정)이 의도와 맞는지 확인 부탁드립니다.
3. calendar.html의 "예정된 공연" 리스트가 선택한 날짜 하루 기준인지, 이번 달 전체 기준인지 확인이 필요합니다 (계획서에는 둘 다 가능하도록 설계했습니다).
4. alarm.html은 특정 공연 하나에 대한 알람 설정 화면이라 진입 시 대상 공연을 어떻게 특정할지(예: calendar.html의 알람 카드 클릭 → `alarm.html?id=alert_001`) 위와 같이 URL 쿼리 파라미터 방식으로 가정했는데 괜찮을지 확인 부탁드립니다.

---

## 19. 신규 화면 확장 계획 (로그인 / 마이페이지 / 상세리포트)

첨부해주신 시안 3장을 기반으로 아래 3개 화면을 추가합니다. 공통 스타일(`common.css`)은 기존 것을 그대로 재사용합니다.

### 19-1. login.html (로그인)
- **상단 브랜딩**: 흰 배경 원 안에 보라색 원형 NOLI 로고(스플래시 로고와 동일한 스타일) + "반가워요!" 타이틀 + "더 즐거운 문화생활의 시작, 노리" 서브 카피
- **일러스트 카드**: 둥근 모서리의 아이보리 톤 카드 안에 구름 위에 앉은 3D 캐�릭터 일러스트 (별·음표 장식 포함)
- **소셜/이메일 로그인 버튼 그룹** (세로 스택)
  - 구글 로그인 — 흰 배경 + 얇은 테두리
  - 카카오 로그인 — 노란색(`#FEE500`) 배경, 어두운 텍스트
  - 네이버 로그인 — 초록색(`#03C75A`) 배경, 흰 텍스트
  - 이메일로 로그인 — 보라 채움 배경 + 편지봉투 아이콘 + 흰 텍스트
- **하단 링크**: "회원가입 · 비밀번호 찾기" (가운데 점 구분자, `flex` 정렬)
- **법적 고지**: "계속 진행함으로써 NOLI의 이용약관 및 개인정보 처리방침에 동의하게 됩니다." (약관/개인정보처리방침 부분 밑줄 링크)

> ⚠️ **확인 필요**
> 1. 실제 소셜 로그인 연동(OAuth)은 백엔드가 필요해 이번 범위에서는 구현할 수 없습니다. 각 버튼 클릭 시 "로그인 성공"으로 간주하고 바로 `index.html`로 이동하는 **mock 처리**로 진행할 예정인데 괜찮을까요?
> 2. 일러스트 이미지는 실제 에셋이 없으므로, 기존 화면들처럼 그라디언트 배경 + 이모지(예: ☁️🎵)로 대체할 예정입니다.

### 19-2. mypage.html (마이페이지)
- **상단 헤더**: 좌측 햄버거 메뉴 / 중앙 음표 아이콘(원형 배지) / 우측 알림 아이콘
- **프로필 카드**
  - 프로필 아바타(원형) + 좌하단 "Lv.12" 배지 오버레이
  - 닉네임 "김노리 님" + 타이틀 뱃지 2개("공연 애호가", "티켓 마스터", pill 형태)
  - 카드 우상단에 작은 원형 장식 아이콘(재생/새로고침 느낌의 아이콘 — 정확한 용도가 불분명해 장식 요소로만 구현)
  - 하단: 선호 장르 아이콘 3개(원형) + "+4"(추가 개수 뱃지) + 우측 "전체보기 >" 링크
- **취향 대시보드**: 섹션 타이틀 아래 카드 2개 나란히
  - "최애 장르" 카드: 아이콘 + "뮤지컬"
  - "큐레이션 매칭률" 카드: 체크 아이콘 + "98%" (강조 배경)
- **나의 공연 기억**: 섹션 타이틀 + "더보기 >", 포스터 카드 2개(가로 배치)
  - 각 카드: 포스터 이미지 + 좌상단 관람일자 배지("23.10.15") + 하단 제목/장소
- **메뉴 리스트** (아이콘 + 텍스트 + `>` chevron, 4개 행): 취향 프로필 수정 / 설정 / 고객 센터 / 공지사항 및 약관
- **로그아웃**: 중앙 정렬 텍스트 링크
- **하단 탭바**: 홈 / 캘린더 / 분석 / 마이(활성) — **4개 탭** (지금까지의 다른 화면들과 또 다른 조합)
- **플로팅 버튼**: 하단 탭바 위에 겹쳐진 보라색 원형 버튼(티켓 아이콘) — 정확한 동작이 불분명해 장식/향후 확장용 UI로만 구현

> ⚠️ **확인 필요**: 하단 탭바 구성이 이번에도 또 다른 조합(홈·캘린더·분석·마이 4개, "탐색"·"보관함" 없음)입니다. 지금까지처럼 각 화면 시안 그대로 유지해도 괜찮을까요? (섹션 9-1, 14-1에서 이미 안내드린 것과 동일한 패턴의 확인 요청입니다.)

### 19-3. analysis.html (상세 리포트)
- **상단 내비게이션**: 뒤로가기(‹) + "상세 리포트" 타이틀(중앙) + 공유 아이콘(우측)
- **관람 트렌드 섹션**
  - 타이틀 "관람 트렌드" + 서브 텍스트 "지난 6개월간의 기록입니다" + 우측 강조 수치 "12 회 관람"
  - 월별(9월~2월) 막대 그래프, 현재 월(2월)은 "NOW" 배지 + 보라색 강조, 나머지는 연한 베이지 톤
- **장르별 상세 분포 섹션**: 도넛 차트(SVG `stroke-dasharray` 기반) 중앙에 "45% 뮤지컬" + 우측 범례(뮤지컬 45% · 콘서트 25% · 전시회 20%)
- **최애 아티스트 랭킹 섹션**: 순위 배지(1, 2) + 프로필 사진(원형) + 이름/역할 + 관람 횟수, 리스트 형태
- **하단 고정 액션바**: "리포트 친구에게 공유하기" 보라 버튼 + 하단 탭바(홈/탐색/캘린더/분석(활성)/프로필 — calendar.html과 동일한 영문 5탭 구성 재사용)

> ⚠️ **확인 필요**: "리포트 친구에게 공유하기" 버튼은 실제 공유 기능(카카오톡 공유 등) 연동이 어려워, 클릭 시 링크를 클립보드에 복사하고 "복사되었습니다" 안내를 띄우는 **mock 공유**로 대체할 예정인데 괜찮을까요?

---

## 20. 신규 화면 반영 — 파일 구조 업데이트

```
noli/
├── ...(기존 파일들)
├── login.html                # 신규
├── mypage.html                # 신규
├── analysis.html              # 신규
├── css/
│   ├── ...(기존 파일들)
│   ├── login.css              # 신규
│   ├── mypage.css             # 신규
│   └── analysis.css           # 신규
└── js/
    ├── data.js                 # 프로필/취향대시보드/공연기억/관람트렌드/장르분포/아티스트랭킹 데이터 필드 추가
    ├── ...(기존 파일들)
    ├── login.js                # 신규
    ├── mypage.js                # 신규
    └── analysis.js              # 신규
```

---

## 21. 신규 화면 반영 — 데이터 구조 확장 (`data.js`)

### 21-1. 사용자 프로필 확장 `user`
```js
{
  name: "김노리",
  avatarUrl: "assets/images/avatar.jpg",
  level: 12,
  titles: ["공연 애호가", "티켓 마스터"],
  favoriteGenreIcons: ["뮤지컬", "콘서트", "전시"], // 상위 3개만 아이콘 노출
  favoriteGenreExtraCount: 4                       // "+4" 표시용
}
```

### 21-2. 취향 대시보드 `tasteDashboard` (신규)
```js
{
  topGenre: "뮤지컬",
  curationMatchRate: 98
}
```

### 21-3. 나의 공연 기억 배열 `memoryPerformances` (신규)
```js
{
  id: "memory_001",
  title: "오페라의 유령",
  venue: "블루스퀘어 신한카드홀",
  viewedDateLabel: "23.10.15",
  posterUrl: "assets/images/phantom.jpg"
}
```

### 21-4. 관람 트렌드 `viewingTrend` (신규)
```js
{
  totalCount: 12,
  months: [
    { label: "9월", count: 6 },
    { label: "10월", count: 3 },
    { label: "11월", count: 8 },
    { label: "12월", count: 9 },
    { label: "1월", count: 5 },
    { label: "2월", count: 7, isCurrent: true }
  ]
}
```

### 21-5. 장르별 상세 분포 `genreDistribution` (신규)
```js
{ genre: "뮤지컬", percentage: 45, color: "#7C5CFC" }
```

### 21-6. 최애 아티스트 랭킹 `topArtistsRanking` (신규)
```js
{ rank: 1, name: "김준수", role: "뮤지컬 배우", count: 4, photoUrl: "assets/images/artists/kimjs.jpg" }
```

### 21-7. 함수 확장
- `getUserProfile()`, `getTasteDashboard()`, `getMemoryPerformances()`
- `getViewingTrend()`, `getGenreDistribution()`, `getTopArtistsRanking()`

---

## 22. 신규 화면 반영 — 단계별 작업 순서 (요청하신 순서대로)

### Step 11. 로그인 화면 (`login.html` / `login.css` / `login.js`)
1. 상단 브랜딩(로고+환영 문구) 마크업
2. 일러스트 카드 마크업(그라디언트+이모지 placeholder)
3. 소셜/이메일 로그인 버튼 4종 마크업 및 스타일(브랜드 컬러 적용)
4. 버튼 클릭 시 mock 로그인 처리 → `index.html` 이동(JS)
5. 하단 링크(회원가입·비밀번호 찾기) + 법적 고지 문구 마크업

### Step 12. 마이페이지 (`mypage.html` / `mypage.css` / `mypage.js`)
1. 상단 헤더 + 프로필 카드(Lv 배지, 타이틀 뱃지, 선호 장르 아이콘) 마크업, `user` 데이터 기반 렌더링
2. 취향 대시보드 카드 2개 마크업, `tasteDashboard` 데이터 렌더링
3. 나의 공연 기억 카드 리스트 렌더링(`memoryPerformances`)
4. 메뉴 리스트(4개 행) + 로그아웃 버튼 마크업 (로그아웃 클릭 시 `splash.html` 또는 `login.html`로 이동)
5. 하단 탭바(홈/캘린더/분석/마이) + 플로팅 버튼 마크업

### Step 13. 상세 리포트 (`analysis.html` / `analysis.css` / `analysis.js`)
1. 상단 내비게이션(뒤로가기/타이틀/공유) 마크업
2. 관람 트렌드 막대 그래프 렌더링(`viewingTrend` 데이터 기반, 현재월 강조)
3. 장르별 분포 도넛 차트(SVG) + 범례 렌더링(`genreDistribution`)
4. 최애 아티스트 랭킹 리스트 렌더링(`topArtistsRanking`)
5. 하단 고정 액션바(공유 버튼 mock 처리) + 하단 탭바(영문 5탭, calendar.html과 동일 패턴 재사용)

---

## 23. 신규 화면(19~22) 확인 요청 사항
1. login.html의 소셜 로그인 버튼은 실제 OAuth 연동 없이, 클릭 시 바로 `index.html`로 이동하는 mock으로 처리할 예정입니다 (19-1 참고).
2. mypage.html의 하단 탭바가 홈/캘린더/분석/마이(4개, 탐색·보관함 없음)로 또 다른 조합인데, 시안 그대로 유지해도 괜찮을까요?
3. mypage.html의 플로팅 티켓 버튼, 프로필 카드의 작은 원형 아이콘은 정확한 동작이 불분명해 장식 요소로만 구현합니다. 실제 기능이 있다면 알려주세요.
4. analysis.html의 "리포트 친구에게 공유하기"는 클립보드 복사 + 안내 문구로 대체할 예정입니다 (19-3 참고).
5. 아바타, 포스터, 아티스트 사진 등 실제 에셋이 없는 항목은 지금까지와 동일하게 플레이스홀더(그라디언트/이니셜 아바타)로 대체합니다.



